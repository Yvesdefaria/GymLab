# GymLab F93 #20/#28/#29/#23 — Plan de implementación

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Cerrar 4 tareas de PLAN.md: verificar/garantizar la cobertura de técnicas de todos los ejercicios (#20), grid de «Más» a 3 columnas (#28), formulario de reporte de errores en Ajustes (#29) y auditar/corregir inputs con valores fijos (#23).

**Architecture:** Cambios bounded sobre código existente. #20 añade un test de cobertura sobre el catálogo (sin cambiar contenido, ya cubierto por T7); #28 es un cambio de clase en `MasPage`; #29 extrae `CONTACT_EMAIL` a un módulo compartido y crea un formulario validado que abre `mailto:`; #23 es un inventario + correcciones puntuales con TDD por corrección.

**Tech Stack:** Vite + React 18 + TS, Tailwind v4, vitest (tests `tests/unit/`), Playwright Python (`tests/e2e/`), i18n `es/core.ts`+`en/core.ts`, `lucide-react`.

**Spec:** Diseños aprobados en chat (2026-09-01). El plan no modifica comportamientos fuera de las 4 tareas.

## Global Constraints

- Verificación por tarea (obligatoria): `npx tsc --noEmit` + `npm run build` + `npm run lint` (solo warnings preexistentes) + `npx vitest run` + Playwright + CHANGELOG + **1 commit sin push**.
- E2E: `python tests/e2e/scripts/with_server.py tests/e2e/test_<fase>.py`; reutiliza dev server en `:5173`; viewports 375×812 (y 768×1024 para #28).
- Copy de UI en español (es-ES); claves i18n SIEMPRE en `es/core.ts` y `en/core.ts`.
- Commits: 1 por tarea, mensaje convencional (`feat:`/`fix:`/`test:`), sin push. NO mezclar drift preexistente de `en/core.ts` (`favoritos: 'Favorites'` sin commitear) ni `--color-warning` de `index.css` — quedan fuera de los commits de estas tareas.
- Tests unit: `npx vitest run`; archivos en `tests/unit/domain/` con import `@/domain/...`.
- No tocar `src/data/seed/exercises.ts` en #20 (los `detailedSteps` propios ya existen; la derivación es en render).

---

### Task 1: Test de cobertura de técnicas de todos los ejercicios (#20)

**Files:**
- Create: `tests/unit/domain/exerciseStepsCoverage.test.ts`
- Modify: `src/i18n/catalog/en.ts` (solo si el test revela huecos EN reales; esperado: sin cambios)
- Test: `tests/unit/domain/exerciseStepsCoverage.test.ts`

**Interfaces:**
- Consumes: `seedExercises` (`@/data/seed/exercises`), `seedExercisesExtra` (`@/data/seed/exercisesExtra`), `STEPS_BY_INSTRUCTION` (`@/i18n/catalog/exerciseSteps`), `STEPS_BY_INSTRUCTION_EN` (`@/i18n/catalog/exerciseStepsEn`), `localizeExerciseDetail` (`@/i18n/catalog/en`).
- Produces: test que garantiza que **todo** ejercicio del catálogo (873) tiene pasos (`detailedSteps` propio o clave derivada en `STEPS_BY_INSTRUCTION`) o fallback `instructions` no vacío, en ES **y** EN.

- [ ] **Step 1: Escribir el test de cobertura**

Crea `tests/unit/domain/exerciseStepsCoverage.test.ts`:

```ts
// Garantiza que TODO ejercicio del catálogo tiene técnica: pasos propios, pasos
// derivados por plantilla de instrucción o fallback de instrucciones no vacío,
// tanto en ES como en EN (la ficha cae a fallback si no hay pasos).
import { describe, expect, it } from 'vitest'
import { seedExercises } from '@/data/seed/exercises'
import { seedExercisesExtra } from '@/data/seed/exercisesExtra'
import { STEPS_BY_INSTRUCTION } from '@/i18n/catalog/exerciseSteps'
import { STEPS_BY_INSTRUCTION_EN } from '@/i18n/catalog/exerciseStepsEn'
import { localizeExerciseDetail } from '@/i18n/catalog/en'
import type { Exercise } from '@/domain/types'

const ALL: Exercise[] = [...seedExercises, ...seedExercisesExtra]

const hasStepsOrFallback = (ex: Exercise): boolean => {
  const derived = STEPS_BY_INSTRUCTION[ex.instructions]
  const steps = ex.detailedSteps ?? derived
  if (steps && steps.length > 0) return true
  return ex.instructions.trim().length > 0
}

describe('cobertura de técnica de todos los ejercicios', () => {
  it('el catálogo completo (seed + extra) tiene pasos o instrucción de fallback en ES', () => {
    const sinTecnica = ALL.filter((ex) => !hasStepsOrFallback(ex))
    expect(sinTecnica).toEqual([])
  })

  it('el catálogo completo tiene técnica en EN vía localizeExerciseDetail', () => {
    const sinEn = ALL.filter((ex) => {
      const localized = localizeExerciseDetail(ex, 'en')
      const steps = localized.detailedSteps
      if (steps && steps.length > 0) return false
      return localized.instructions.trim().length === 0
    })
    expect(sinEn).toEqual([])
  })

  it('toda clave de plantilla ES tiene su traducción EN equivalente', () => {
    const sinEn = Object.keys(STEPS_BY_INSTRUCTION).filter(
      (k) => !STEPS_BY_INSTRUCTION_EN[k],
    )
    expect(sinEn).toEqual([])
  })

  it('los pasos derivados tienen 2-6 pasos con step correlativo y texto no vacío', () => {
    for (const [inst, steps] of Object.entries(STEPS_BY_INSTRUCTION)) {
      expect(steps.length).toBeGreaterThanOrEqual(2)
      expect(steps.length).toBeLessThanOrEqual(6)
      steps.forEach((s, i) => expect(s.step).toBe(i + 1))
      expect(steps.every((s) => s.instruction.trim().length > 0)).toBe(true)
      expect(inst.trim().length).toBeGreaterThan(0)
    }
  })
})
```

- [ ] **Step 2: Ejecutar el test y ver que falla o pasa**

Run: `npx vitest run tests/unit/domain/exerciseStepsCoverage.test.ts`
Expected: los 4 tests pasan (la cobertura T7 ya está). Si algún caso falla, documenta cuál y corrígelo en `exerciseSteps.ts`/`exerciseStepsEn.ts` añadiendo la clave/plantilla que falta (mismo formato).

- [ ] **Step 3: Ejecutar la suite completa**

Run: `npx vitest run`
Expected: todos los tests pasan (290 previos + 4 nuevos).

- [ ] **Step 4: Verificación E2E de fichas con técnica derivada**

Crea `tests/e2e/test_f93_t20_technique.py` (patrón de `test_f93_t18_common.py`): abre `/ejercicios/ejercicio-balón-crunch` (id 1242, sin `detailedSteps` en seed) y un ejercicio curado con pasos propios (`/ejercicios/press-de-pecho-con-barra`), y verifica que la ficha muestra el bloque «Técnica» con lista numerada (números 1..n visibles) y que el fallback NO aparece como único contenido. 0 errores de consola.

Ejecutar: `python tests/e2e/scripts/with_server.py tests/e2e/test_f93_t20_technique.py`
Expected: ALL OK.

- [ ] **Step 5: Verificación completa + CHANGELOG + commit**

Run: `npx tsc --noEmit`; `npm run build`; `npm run lint` (solo warnings preexistentes); `npx vitest run`.
Añade entrada en `CHANGELOG.md` bajo `[Unreleased]` → `### Added`: test de cobertura de técnica para todo el catálogo (ES+EN), garantizando pasos propios/derivados/fallback.
Commit:
```bash
git add gymlab-app/tests/unit/domain/exerciseStepsCoverage.test.ts gymlab-app/tests/e2e/test_f93_t20_technique.py gymlab-app/CHANGELOG.md
git commit -m "test: cobertura de tecnica para todo el catalogo de ejercicios (F93 #20)"
```
NO incluir `en/core.ts` ni `index.css` (drift). Sin push.

---

### Task 2: Grid de «Más» a 3 columnas (#28)

**Files:**
- Modify: `src/pages/MasPage.tsx:156-169` (bloque grip)
- Create: `tests/e2e/test_f93_t28_mas_grid.py`

**Interfaces:**
- Consumes: `links` (14 entradas del catálogo de MasPage), `settings.hubLayout`.
- Produces: grip con `grid-cols-3`, sin overflow a 320px, targets ≥44px.

- [ ] **Step 1: Cambiar el grip a 3 columnas**

En `src/pages/MasPage.tsx`, dentro del bloque `isGrip` (línea ~156), cambia `grid-cols-2` → `grid-cols-3` y ajusta densidad para que las etiquetas largas no desborden en 320px:

```tsx
<div className="grid grid-cols-3 gap-2">
  {links.map(({ to, labelKey, icon: Icon }, i) => (
    <Link
      key={to}
      to={to}
      className={`stagger-fade stagger-fade-${Math.min(i + 1, 8)} flex min-h-[72px] flex-col items-center justify-center gap-1.5 rounded-2xl border-b border-border/30 px-1 py-3 text-center transition-colors hover:border-gold/70`}
    >
      <span className="flex size-10 items-center justify-center rounded-xl bg-bg-elevated text-accent">
        <Icon className="size-5" aria-hidden />
      </span>
      <span className="line-clamp-2 block text-xs font-medium leading-tight text-fg">{t(labelKey)}</span>
    </Link>
  ))}
</div>
```

Notas: `min-h-[72px]` garantiza target ≥44px + contenido; `line-clamp-2` + `text-xs` evitan overflow con «Biblioteca de ejercicios»/«Progreso de fotos» en 320px.

- [ ] **Step 2: Verificar tsc/build**

Run: `npx tsc --noEmit`; `npm run build`.
Expected: limpio.

- [ ] **Step 3: E2E del grid a 320px y tablet**

Crea `tests/e2e/test_f93_t28_mas_grid.py` (patrón `test_f93_t18_common.py`, pero navegando a `/mas` directamente). Con viewports **320×700**, **375×812** y **768×1024**:
- el modo grip (por defecto) muestra 3 columnas: compara `x` de 3 enlaces consecutivos (difieren en X) y que el 4º arranca en la columna 1;
- comprueba `document.documentElement.scrollWidth <= window.innerWidth` (sin overflow horizontal);
- los enlaces del hub tienen altura ≥44px (`bounding_box().height >= 44`);
- 0 errores de consola.

Ejecutar: `python tests/e2e/scripts/with_server.py tests/e2e/test_f93_t28_mas_grid.py`
Expected: ALL OK.

- [ ] **Step 4: Verificación completa + CHANGELOG + commit**

Run: `npx vitest run`; `npm run lint`.
Añade en CHANGELOG `### Changed`: hub «Más» grip de 3 columnas (T9/F43), sin overflow a 320px, targets ≥44px.
Commit:
```bash
git add gymlab-app/src/pages/MasPage.tsx gymlab-app/tests/e2e/test_f93_t28_mas_grid.py gymlab-app/CHANGELOG.md
git commit -m "feat: grid de 3 columnas en el hub Mas (F93 #28)"
```
Sin push.

---

### Task 3: Formulario de reporte de errores en Ajustes (#29)

**Files:**
- Create: `src/config/contact.ts`
- Create: `src/components/settings/ReportBugSection.tsx`
- Modify: `src/pages/AjustesPage.tsx` (añadir `<ReportBugSection />`)
- Modify: `src/pages/TerminosPage.tsx` (usar `CONTACT_EMAIL` importado)
- Modify: `src/components/settings/index.ts` (exportar `ReportBugSection`)
- Modify: `src/i18n/locales/es/core.ts` y `src/i18n/locales/en/core.ts` (claves `ajustes.reporte*`)
- Create: `tests/unit/domain/reportBug.test.ts`
- Create: `tests/e2e/test_f93_t29_report_bug.py`

**Interfaces:**
- Produces: `src/config/contact.ts` exporta `export const CONTACT_EMAIL = 'gymlab@app.gymlab.dev'` (placeholder; se rellena en #30). `ReportBugSection` usa `useTranslation`, `useSettings` (para versión/no), y genera un `mailto:` al enviar.

- [ ] **Step 1: Escribir el test unit de validación del reporte**

Crea `tests/unit/domain/reportBug.test.ts` — valida la construcción del cuerpo del mailto (función pura en `src/domain/report.ts`, creada en Step 2):

```ts
// Valida la construcción del reporte de errores: campos, validación del cuerpo
// y generación del mailto con el reporte preformateado.
import { describe, expect, it } from 'vitest'
import { buildReportBody, validateReport } from '@/domain/report'

describe('validateReport', () => {
  it('rechaza descripción vacía o demasiado corta', () => {
    expect(validateReport('', 'error').valid).toBe(false)
    expect(validateReport('corto', 'error').valid).toBe(false)
  })

  it('acepta descripción con mínimo de 10 caracteres y tipo válido', () => {
    expect(validateReport('La app se cierra al registrar', 'error').valid).toBe(true)
  })
})

describe('buildReportBody', () => {
  it('incluye tipo, descripción y (si hay) email de contacto', () => {
    const body = buildReportBody('error', 'La app se cierra al registrar', 'user@x.com')
    expect(body).toContain('La app se cierra al registrar')
    expect(body).toContain('user@x.com')
  })
})
```

- [ ] **Step 2: Crear el dominio puro del reporte**

Crea `src/domain/report.ts` (sin React, arquitectura por capas):

```ts
// Dominio del formulario de reporte de errores: validación y cuerpo del correo.

export type ReportType = 'error' | 'mejora' | 'otro'

export const REPORT_MIN_LENGTH = 10

export interface ReportInput {
  type: ReportType
  description: string
  email?: string
}

export interface ReportValidation {
  valid: boolean
  error?: string
}

// La descripción es obligatoria (mín. 10 caracteres); el email opcional.
export const validateReport = (description: string, _type: ReportType): ReportValidation => {
  const trimmed = description.trim()
  if (trimmed.length < REPORT_MIN_LENGTH) {
    return { valid: false, error: 'reporte.errorDescripcion' }
  }
  return { valid: true }
}

// Cuerpo del correo con el reporte preformateado (tipo, descripción, email).
export const buildReportBody = (type: ReportType, description: string, email?: string): string => {
  const lines = [
    `Tipo: ${type}`,
    `Descripción: ${description.trim()}`,
  ]
  if (email?.trim()) lines.push(`Contacto: ${email.trim()}`)
  return lines.join('\n')
}
```

- [ ] **Step 3: Crear la constante de contacto compartida**

Crea `src/config/contact.ts`:

```ts
// Correo de contacto de la app. Es un PLACEHOLDER: la cuenta aún no existe y se
// creará/reemplazará en la fase #30. Lo consumen los T&C y el reporte de errores.
export const CONTACT_EMAIL = 'gymlab@app.gymlab.dev'
```

- [ ] **Step 4: Crear el componente ReportBugSection**

Crea `src/components/settings/ReportBugSection.tsx` siguiendo el patrón de `DataSection` (section `panel-light rounded-2xl p-4` con título `font-display`). Estados: tipo (chips `aria-pressed` con las 3 opciones), textarea (obligatorio, `minLength={10}`), email opcional (`type="email"`), estado de envío (mensaje de éxito). Al submit, si `validateReport` falla muestra el error i18n; si pasa, genera `window.location.href = mailto:${CONTACT_EMAIL}?subject=...&body=${encodeURIComponent(body)}` y muestra confirmación «reporte.enviado». Añade `aria-invalid` y `role="alert"` para errores.

Añade a `src/components/settings/index.ts`:
```ts
export { ReportBugSection } from './ReportBugSection'
```

- [ ] **Step 5: Conectar en AjustesPage y TerminosPage**

En `src/pages/AjustesPage.tsx`, importa y añade `<ReportBugSection />` tras `<DataSection />`. En `src/pages/TerminosPage.tsx`, elimina el `const CONTACT_EMAIL` local (línea 12) y usa `import { CONTACT_EMAIL } from '@/config/contact'`.

- [ ] **Step 6: i18n es/en**

En `src/i18n/locales/es/core.ts` dentro de `ajustes:` añade:
```ts
reporteTitulo: 'Reportar un error',
reporteSubtitulo: 'Cuéntanos qué ha pasado para poder arreglarlo.',
reporteTipo: 'Tipo de reporte',
reporteTipoError: 'Error',
reporteTipoMejora: 'Mejora',
reporteTipoOtro: 'Otro',
reporteDescripcion: 'Descripción',
reporteDescripcionPlaceholder: 'Describe qué ha pasado (mín. 10 caracteres)…',
reporteEmail: 'Email de contacto (opcional)',
reporteEmailPlaceholder: 'tucorreo@ejemplo.com',
reporteEnviar: 'Enviar reporte',
reporteEnviado: 'Gracias. Se ha abierto tu correo con el reporte listo para enviar.',
reporteErrorDescripcion: 'Describe el problema con al menos 10 caracteres.',
```
En `src/i18n/locales/en/core.ts` añade las mismas claves traducidas al inglés (p. ej. `reporteTitulo: 'Report a bug'`, etc.).

- [ ] **Step 7: Tests unit**

Run: `npx vitest run tests/unit/domain/reportBug.test.ts` → PASS. Luego `npx vitest run` completa.

- [ ] **Step 8: E2E del formulario**

Crea `tests/e2e/test_f93_t29_report_bug.py` (patrón `test_f93_t18_common.py`): navega a `/ajustes`, despliega/sección «Reportar un error» visible; deja descripción corta → error `role="alert"` visible; rellena descripción válida → botón «Enviar reporte»; tras click se muestra «reporteEnviado» (y el mailto cambia `location` — verifica que el mensaje de éxito aparece antes del salto, o intercepta el mailto con un listener `window.location` stub en el test). 0 errores de consola.

Ejecutar: `python tests/e2e/scripts/with_server.py tests/e2e/test_f93_t29_report_bug.py` → ALL OK.

- [ ] **Step 9: Verificación + CHANGELOG + commit**

Run: `npx tsc --noEmit`; `npm run build`; `npm run lint`; `npx vitest run`.
Añade CHANGELOG `### Added`: formulario de reporte de errores en Ajustes (tipo + descripción + email opcional → mailto a `CONTACT_EMAIL` placeholder, extraído a `src/config/contact.ts`), dominio `report.ts` validado.
Commit:
```bash
git add gymlab-app/src/config/contact.ts gymlab-app/src/domain/report.ts gymlab-app/src/components/settings/ReportBugSection.tsx gymlab-app/src/components/settings/index.ts gymlab-app/src/pages/AjustesPage.tsx gymlab-app/src/pages/TerminosPage.tsx gymlab-app/src/i18n/locales/es/core.ts gymlab-app/src/i18n/locales/en/core.ts gymlab-app/tests/unit/domain/reportBug.test.ts gymlab-app/tests/e2e/test_f93_t29_report_bug.py gymlab-app/CHANGELOG.md
git commit -m "feat: formulario de reporte de errores en ajustes (F93 #29)"
```
Sin push. NOTA: si `en/core.ts` tiene el drift `favoritos: 'Favorites'`, usa el patrón de swap para que el commit lleve `'Favourites'` (ver #18/#19).

---

### Task 4: Auditar y corregir inputs con valores fijos (#23)

**Files:**
- Modify: los archivos que señale el inventario (probablemente calculadoras: `AguaPage.tsx`, `ImcPage.tsx`, `CaloriasPage.tsx`, `OneRepMaxPage.tsx`, `GrasaCorporalPage.tsx`, `MedidasCorporalesPage.tsx`, y verificar `EntrenamientoPage.tsx`/`SesionPage.tsx`/`RutinaBuilderPage.tsx`)
- Create: `tests/e2e/test_f93_t23_inputs.py`

**Interfaces:**
- Consumes: el inventario de inputs por página (Step 1). Prohibido tocar inputs ya correctos (placeholders/`min`/`max` sensatos y sin `value=` hardcodeado).

- [ ] **Step 1: Inventario de inputs con valores fijos**

Recorre las páginas de calculadoras y sesión. Para cada `<input>` con `type="number"`/`text`/`email` documenta en el reporte de la tarea:
- `value={...}` (¿controlado con estado o literal hardcodeado?),
- `placeholder`, `min`, `max`, `step` (¿valores sensatos o arbitrarios?),
- validación actual (¿`required`/`minLength`/`onChange` parse?).
Criterio de corrección: un input está mal si **preselecciona un valor de negocio hardcodeado** (p. ej. `value={0}` o `value={70}` sin estado) que contamine el cálculo al abrir la página, o si `min/max/step` bloquean valores legítimos. Un `placeholder` informativo o `min/max` de rango válido NO se corrige.

- [ ] **Step 2: Corregir los inputs reales (TDD por corrección)**

Para cada input que cumpla el criterio: primero escribe/ajusta el test que lo reproduce (el valor fijo contamina el cálculo), después corrige. Patrones:
- Quitar `value={0}`/`value={número}` preseleccionados → estado inicial vacío (`''`) + `placeholder` + parse seguro (`Number('')` → NaN manejado).
- Asegurar `min/max` no excluyan valores válidos (p. ej. altura `min={50}` cm está bien; si algo queda fuera, ajustar a rango realista).
- Validar al enviar (descripción del error i18n) en vez de bloquear al teclear.
Usa el hook `useDebouncedValue`/estado local según el patrón de cada página. Ejecuta `npx vitest run` tras cada corrección (suite completa debe pasar).

- [ ] **Step 3: E2E por página**

Crea `tests/e2e/test_f93_t23_inputs.py` (patrón `test_f93_t18_common.py`): para cada calculadora corregida verifica que (a) el input arranca **vacío** (placeholder visible, sin `0` prellenado) cuando el cálculo no debe tener valor por defecto, (b) introducir un dato válido calcula sin errores, (c) un dato fuera de rango muestra validación/error i18n y no rompe, (d) 0 errores de consola.

Ejecutar: `python tests/e2e/scripts/with_server.py tests/e2e/test_f93_t23_inputs.py` → ALL OK.

- [ ] **Step 4: Verificación + CHANGELOG + commit**

Run: `npx tsc --noEmit`; `npm run build`; `npm run lint`; `npx vitest run`.
Añade CHANGELOG `### Fixed`: auditar inputs con valores fijos — lista los inputs corregidos (página + qué se cambió) y confirma que los placeholders/rangos sensatos se conservan.
Commit:
```bash
git add <archivos corregidos> gymlab-app/tests/e2e/test_f93_t23_inputs.py gymlab-app/CHANGELOG.md
git commit -m "fix: auditar inputs con valores fijos en calculadoras y sesion (F93 #23)"
```
Sin push.

---

## Self-Review

**Cobertura del spec:** #20 → Task 1 (test de cobertura ES+EN + E2E de fichas); #28 → Task 2 (grid 3 col + 320px/tablet); #29 → Task 3 (formulario + dominio + mailto placeholder + i18n); #23 → Task 4 (inventario + correcciones TDD + E2E). Sin huecos.

**Placeholders:** Ningún TBD. Los `min/max` de ejemplo en Task 4 son guías (el inventario decide), pero el criterio de corrección es explícito.

**Consistencia de tipos:** `ReportType`/`validateReport`/`buildReportBody` en Task 3 se definen en `report.ts` y se usan idénticos en el test; `CONTACT_EMAIL` se define una vez y se importa en `TerminosPage` y `ReportBugSection`.

**Drift:** `en/core.ts` y `index.css` quedan fuera de los commits (constraint global).
---

### Task 5: Rehacer #20 — tips/warnings en todas las fichas + checklist de sesión ampliada

> Sustituye la Task 1 como cierre de #20. La Task 1 (test de cobertura) ya está commiteada (867f221) y se MANTIENE, pero se refuerza para exigir tips/warnings.

**Files:**
- Modify: src/i18n/catalog/exerciseSteps.ts (añadir tip/warning a las 31 plantillas + claves de curados)
- Modify: src/i18n/catalog/exerciseStepsEn.ts (ídem EN)
- Modify: src/domain/techniqueData.ts (ampliar de 5 patrones a todos los grupos musculares)
- Modify: src/components/session/TechniqueChecklist.tsx (fallback a pasos derivados)
- Modify: src/i18n/locales/es/core.ts y src/i18n/locales/en/core.ts (claves 	echnique.* nuevas)
- Modify: 	ests/unit/domain/exerciseStepsCoverage.test.ts (exigir ≥1 tip/warning por plantilla)
- Modify: 	ests/e2e/test_f93_t20_technique.py (verificar tip/warning en ficha ampliada + modal de sesión con fallback)
- Modify: CHANGELOG.md

**Interfaces:**
- Consumes: ExerciseStep ({step, instruction, tip?, warning?}), ExerciseTechniqueCard (ya renderiza tip/warning), TechniqueChecklist (usa getTechniqueChecklist + useExerciseCatalog + localizeExerciseDetail para el fallback), STEPS_BY_INSTRUCTION/STEPS_BY_INSTRUCTION_EN.
- Produces: toda ficha (873) con ≥1 tip o warning; TechniqueChecklist sin «No hay datos» para ejercicios con pasos derivados.

- [ ] **Step 1: Añadir tip/warning a las plantillas derivadas (ES)**

En src/i18n/catalog/exerciseSteps.ts, para CADA una de las 31 plantillas genéricas añade 	ip y/o warning en los pasos donde encaje (setup/agarre/espalda; el error típico del movimiento). Patrón tipo banca:
`	s
'FLEXIONA LOS CODOS llevando la carga...': [
  { step: 1, instruction: '...', tip: 'Aprieta los codos al cuerpo para aislar el bíceps.' },
  { step: 2, instruction: '...' },
  { step: 3, instruction: '...', warning: 'No balancees el torso: usa un peso controlable.' },
  { step: 4, instruction: '...' },
],
`
Misma técnica para las ~40 claves de curados con instrucción única (tips específicos del ejercicio). Mínimo: **cada plantilla debe tener ≥1 tip o warning** en ES.

- [ ] **Step 2: ídem EN**

En src/i18n/catalog/exerciseStepsEn.ts, traduce los tips/warnings añadidos. Mínimo: cada plantilla EN con ≥1 tip o warning.

- [ ] **Step 3: Reforzar el test de cobertura**

En 	ests/unit/domain/exerciseStepsCoverage.test.ts añade:
`	s
it('cada plantilla derivada tiene al menos un tip o warning (ES)', () => {
  for (const [inst, steps] of Object.entries(STEPS_BY_INSTRUCTION)) {
    expect(
      steps.some((s) => (s.tip?.trim().length ?? 0) > 0 || (s.warning?.trim().length ?? 0) > 0),
      plantilla sin tip/warning: ,
    ).toBe(true)
  }
})
it('cada plantilla derivada tiene al menos un tip o warning (EN)', () => {
  for (const [inst, steps] of Object.entries(STEPS_BY_INSTRUCTION_EN)) {
    expect(
      steps.some((s) => (s.tip?.trim().length ?? 0) > 0 || (s.warning?.trim().length ?? 0) > 0),
      plantilla EN sin tip/warning: ,
    ).toBe(true)
  }
})
`
Ejecutar 
px vitest run tests/unit/domain/exerciseStepsCoverage.test.ts → debe fallar (ROJO) hasta rellenar todas las plantillas.

- [ ] **Step 4: Ampliar TECHNIQUE_DATA a todos los grupos**

En src/domain/techniqueData.ts, añade patrones (slug regex) para cubrir al menos: curl (bíceps), extensiones/press francés (tríceps), elevaciones laterales/frontales (hombro), dominadas/jalón/pullover (espalda), prensa/extensión de piernas/curl femoral/pantorrilla (pierna), plancha/crunch/elevación de piernas (abdomen), encogimientos/remo al mentón (trapecios), hip thrust/glúteo, fondos, flexiones. Cada uno con 3-4 puntos labelKey/descriptionKey (nuevas claves i18n).

- [ ] **Step 5: i18n de los nuevos puntos**

Añade en src/i18n/locales/es/core.ts y en/core.ts (bloque 	echnique:) las claves de label/descripción de los nuevos puntos (es-ES + inglés).

- [ ] **Step 6: Fallback en TechniqueChecklist**

En src/components/session/TechniqueChecklist.tsx, si getTechniqueChecklist no devuelve checklist, resuelve los pasos del ejercicio: useExerciseCatalog() → ind por slug → localizeExerciseDetail(ex, lang).detailedSteps. Si hay pasos, muéstralos como lista numerada (mismo estilo que la checklist); si no hay ni checklist ni pasos, mantener «No hay datos» (	echnique.noData).

- [ ] **Step 7: Tests unit + E2E**

Run: 
px vitest run (suite completa, incluye los nuevos casos de tip/warning). Actualiza 	ests/e2e/test_f93_t20_technique.py: verifica que una ficha ampliada (exercise-ball-crunch) muestra un tip o warning renderizado (icono Lightbulb/AlertTriangle o texto), y que el modal de técnica de la sesión para un ejercicio sin checklist propia muestra los pasos derivados (no «No hay datos»). E2E: python tests/e2e/scripts/with_server.py tests/e2e/test_f93_t20_technique.py → ALL OK.

- [ ] **Step 8: Verificación completa + CHANGELOG + commit**

Run: 
px tsc --noEmit; 
pm run build; 
pm run lint; 
px vitest run.
Añade CHANGELOG (edita la entrada existente de #20 en ### Added): tips/warnings en todas las plantillas de técnica (ES+EN), checklist de técnica de sesión ampliada a todos los grupos con fallback a pasos derivados.
Commit (1 commit, sin push):
`ash
git add src/i18n/catalog/exerciseSteps.ts src/i18n/catalog/exerciseStepsEn.ts src/domain/techniqueData.ts src/components/session/TechniqueChecklist.tsx src/i18n/locales/es/core.ts src/i18n/locales/en/core.ts tests/unit/domain/exerciseStepsCoverage.test.ts tests/e2e/test_f93_t20_technique.py CHANGELOG.md
git commit -m "feat: tips y warnings de tecnica en todas las fichas + checklist de sesion ampliada (F93 #20)"
`
NOTA drift: si tocas en/core.ts (claves technique), usa el swap avoritos: 'Favorites'↔'Favourites' para que el commit lleve 'Favourites' y el working copy quede con 'Favorites'. No tocar index.css.
