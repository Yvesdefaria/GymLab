# Changelog — GymLab App

Formato basado en [Keep a Changelog](https://keepachangelog.com/es-ES/1.1.0/).
El versionado sigue [SemVer](https://semver.org/lang/es/) cuando haya releases formales.

---

## [Unreleased]

### Changed
- **Vocabulario de rutinas en `src/domain/catalog.ts` (`refactor`, Fase 47·R2)**: `OBJECTIVES`/`LEVELS` (valores) y `OBJECTIVE_LABELS_ES/EN`/`LEVEL_LABELS_ES/EN` pasan a `domain/catalog.ts`; `types.ts` deriva los tipos `Objective`/`Level` de esas listas y `domain/routines.ts` queda solo con `slugify` (desaparecen `OBJECTIVE_LABELS`/`LEVEL_LABELS`). Consumidores actualizados: `RutinasPage` (3 arrays inline → `OBJECTIVES`/`LEVELS`), `RutinaBuilderPage` (`objectiveOptions`/`levelOptions` → catálogo), `steps.tsx` (onboarding; valores derivados del catálogo + claves i18n en `Record`) y `i18n/catalog/en.ts` (eliminados `OBJECTIVE_LABELS_EN`/`LEVEL_LABELS_EN` locales). Nota: el orden de los chips de objetivo en el onboarding pasa al del catálogo (volumen primero). Verificado: tsc, 71 tests, lint limpio en `src/` y build.
- **Vocabulario de ejercicios en `src/domain/catalog.ts` (`refactor`, Fase 47·R1)**: nueva única fuente de verdad para grupos musculares, equipamiento y categorías con labels ES/EN. `types.ts` deriva los tipos (`MuscleGroup`, `Equipment`, `ExerciseCategory`) desde las listas del catálogo; `ExerciseFilterBar`, `MuscleDummy` y `useExerciseCatalog` consumen esas mismas listas/labels; `i18n/catalog/en.ts` elimina los mapas locales `MUSCLE_GROUP_LABELS_EN`/`EQUIPMENT_LABELS_EN`/`CATEGORY_LABELS_EN` y `domain/exerciseCategory.ts` deja de duplicar `CATEGORY_LABELS`. Se eliminan `MUSCLE_GROUP_LABELS` (muerto, `domain/routines.ts`) y `categoryLabel` (muerto, `useExerciseCatalog.ts`); se resuelve la deriva `'cardio'` (músculo) y `'cuerda'` (equipo), valores solo en el tipo y sin uso en seeds ni en `exercises-v1.json`, retirados del vocabulario. Los chips de categoría del filtro pasan de `t()` a `localizeCategory` (se retiran 4 claves i18n huérfanas) y los labels de músculo en ES se capitalizan (antes se mostraba el valor crudo). Verificado: tsc, 71 tests, lint limpio en `src/` y build.
- **Lint `src/` sin warnings (`refactor`)**: se eliminan los 15 warnings de oxlint del código de la app. (1) `react-hooks(exhaustive-deps)` — el patrón `useLiveQuery(...) ?? []` creaba un array nuevo en cada render y desestabilizaba las dependencias de `useMemo`/`useCallback`; ahora los hooks usan constantes de módulo vacías estables (`useBodyWeight`, `useBodyMeasurements`, `useSkinfolds`, `useExerciseFavorites`, `useRoutineFavorites`, `useExerciseDetail`, `useLoadSuggestion`). (2) `react(only-export-components)` — las constantes y funciones exportadas junto a componentes de onboarding se mueven a dominio/lib: `MATERIALS`/`HEIGHT_RANGE`/`WEIGHT_RANGE` a `src/domain/onboarding.ts`; `isSafeAvatarUri` (y allowlists) a `src/lib/avatar.ts`; `StatsRange`/`inRange` a `src/domain/dates.ts` (actualizados los 4 charts); `MUSCLE_GROUP_ICONS` deja de exportarse (uso interno). (3) `CountUp` leía `display` dentro del efecto sin declararlo en deps — se captura el valor de partida con una ref. Verificado: lint limpio en `src/`, tsc, 71 tests y build.

### Added
- **Sombra degradada + foco de luz en cards (`T8`)**: `src/index.css` añade el nivel 3 de elevación `.panel-elevated` (foco de luz cenital en la esquina superior-izquierda con `radial-gradient` + sombra profunda, transición suave de `box-shadow` en hover/active). `.routine-card` gana el mismo foco de luz sobre el velo de legibilidad (`::before`) y transición de `box-shadow` en `:hover` (elevación) y `:active` (presión táctil). Se aplica `.panel-elevated` a la card de información principal de `RutinaDetailPage`. **Refuerzo visual (2.ª pasada)**: sobre OLED negro una sombra negra es invisible, así que el halo pasa a ser **dorado** (`color-mix` con `--color-gold`) — visible como resplandor cálido alrededor de la card — y el foco cenital del velo se intensifica (blanco 42%→55% en el origen) para leerse incluso sobre las fotos de rutinas (muy oscuras). Verificado: tsc, build, lint y Playwright 375×812 + 768×1024 (`test_t8.py`: sombra + foco en cards, `.panel-elevated` en detalle, cards centradas respecto al shell en tablet; render verificado por píxeles en la card real).
- **Catálogo EN en overlay de render (`feat`, Fase 45·B3)**: se añade `src/i18n/catalog/` con la capa de traducción EN del catálogo que se aplica **en render** (Dexie y el seed siguen guardando en español, sin cambios de modelo ni de `SEED_VERSION`). Datos: `exerciseNamesEn.ts` (nombres EN de los 821 ejercicios del catálogo generados desde su `externalId`, que ya es EN), `instructionTemplatesEn.ts` (31 plantillas de instrucciones ES→EN con cobertura verificada 0 missing), `curatedExercisesEn.ts` (52 ejercicios curados con nombre/instrucciones y `detailedSteps` con tip/warning), `routinesEn.ts` (23 rutinas con título/descripción + `ROUTINE_DAYS_EN` por id de día), `guidesEn.ts` (18 guías completas) y `papersEn.ts` (6 papers). Módulo `en.ts` con helpers `localizeExercise`/`localizeExerciseDetail`/`localizeRoutine`/`localizeRoutineDay`/`localizeGuide`/`localizePaper` y labels traducidos de `muscleGroup`, `equipment`, `category`, `objective`, `level`, categoría de guía y tema de paper. **Aplicado en las páginas de catálogo/detalle**: `EjerciciosPage` y `ExercisePicker` (nombres localizados antes de filtrar para que la búsqueda coincida en el idioma activo), `EjercicioDetailPage` (título, subtítulo, técnica y pasos), `RutinasPage`, `RutinaDetailPage` (títulos, descripción, objetivo, nivel, nombres de día y de ejercicio vía `useRoutineDetail`/`useRoutineDayItems`), `RutinaBuilderPage`, `GuiasPage`, `GuiaDetailPage`, `PapersPage` y `PaperDetailPage`; charts `VolumeByMuscleChart`/`VolumeByMuscleDonut` y `ExerciseFilterBar` con los labels traducidos. Verificado: tsc, 71 tests, lint, build y Playwright 375×812 + 768×1024 en es y en (`test_f45b3.py`, que comprueba el overlay en catálogo/detalle de ejercicios, rutinas, guías y papers).
- **i18n completa de la UI (`feat`, Fase 45·B2)**: migrados todos los textos hardcoded de la UI a claves `t()` con `react-i18next` (es-ES por defecto, plurals con `count`). `src/i18n/locales/es.ts` es ahora la fuente única de claves (817 claves en 26 namespaces, cobertura total validada con un script de paridad: 0 claves usadas sin definir) y `en.ts` replica la misma estructura tipada contra `EsSchema` (si falta una clave en cualquier idioma, falla el typecheck). Correcciones de cobertura: `rutinas.anadirFavAria`/`quitarFavAria` con interpolación `{{title}}`, typo `mejoMarca`→`mejorMarca`, `workout.cerrado`→`workout.cerrar`, se eliminaron claves inventadas (`papers.buscarPlaceholder`/`buscarAria`, `stats.disclaimer`) y se añadieron las claves reales que el código usaba y no existían (paletas de color, descripciones del hub de calculadoras, sexo Hombre/Mujer, rangos 30 d/90 d/Todo, días de la semana L/M/X/J/V/S/D, 18 descripciones SEO, `onboarding.terminos` como Trans, etc.). Nuevo tipo `I18nKey` (dot-path de `ParseKeys`) en `src/i18n/index.ts` para tipar arrays/records de claves dinámicas que se pasan a `t()` (`useSeo.ts`, `AjustesPage`, onboarding, `BodyWeightChart`). Interpolaciones frágiles verificadas (p. ej. `session.subtitulo` con `count`+`completadas`+`total`). Verificado: tsc, 71 tests, lint, build y (Pendiente: Playwright es/en).
- **Infraestructura i18n (`feat`, Fase 45·B1)**: instalados `i18next@26` + `react-i18next@17`. Nuevo `src/domain/settings.ts` con `language: 'es' | 'en'` en `AppSettings` (por defecto `es`). `src/i18n/index.ts` configura i18next (es-ES por defecto, sin detección automática, `initAsync: false`) con `applyLanguage()` que cambia el idioma, fija `document.documentElement.lang` y el título base por idioma; `src/i18n/locales/es.ts` (fuente de claves) y `en.ts` tipado contra su esquema (`EsSchema`) para que falte cualquier clave y falle el typecheck. El gate de bootstrap en `src/app/providers.tsx` lee `settings.language` tras `ensureSeeded()` y aplica el idioma antes de render (sin parpadeo). Nuevo `src/lib/intl.ts` con `formatDate`/`formatNumber`/`formatVolume` por locale (es-ES/en-GB, 3 tests). Selector **Idioma** en `AjustesPage` (apica al instante y persiste en Ajustes) y el onboarding aplica el idioma elegido al instante y lo guarda en `settings.language` al terminar. Verificado: tsc, 71 tests, lint, build y Playwright 375×812 + 768×1024 (`test_f45.py`: es por defecto, cambio a en con `<html lang="en">` + título en inglés, y persistencia tras recargar).
- **Onboarding con datos útiles (`feat`, Fase 44)**: el asistente de bienvenida deja de quedarse solo en `onboardingAnswers` y alimenta meta, perfil y calculadoras. Nuevo `src/domain/profileMeta.ts` (dominio puro) con las constantes `HEIGHT_KEY`/`BODY_SEX_KEY`/`BIRTH_DATE_KEY` y `weeklyGoalFromDays()` (5 tests nuevos); `finish()` en `Onboarding.tsx` escribe en `meta` altura/sexo/fecha de nacimiento **solo si son válidos**, hace `bodyWeightRepo.upsert` con el peso inicial, crea el perfil (`profileRepo.ensure`) con `weeklyGoal` derivado de los días/semana y no reescribe si la sesión ya está completada. Nuevos hooks `useProfileAge` (edad derivada de `meta.birthDate` reusando `ageFromBirthDate`) y `useAgePrefill` (pre-rellena la edad de `CaloriasPage`, `MacrosPage` y `GrasaCorporalPage` respetando lo que el usuario ya tecleó y los registros guardados de hoy); el paso Perfil del onboarding (`ProfileStep`) acepta el peso en libras cuando `units === 'lb'` (valida siempre en kg con `parseWeightToKg` y convierte el valor al cambiar de unidad) y `AjustesPage` sincroniza ahora `measurementSystem` al cambiar kg/lb. Estadísticas, medidas y grasa corporal unifican sus claves de perfil importando `HEIGHT_KEY`/`BODY_SEX_KEY` (se acabaron los literales duplicados). Verificado: tsc, 68 tests, build y Playwright 375×812 + 768×1024 (`test_f44.py`: flujo completo en lb → `meta`/`bodyWeight`/`profile`/programa activo y home «Empezar hoy»; skip → sin datos y home «Sin plan hoy»).
- **Charts accesibles (`feat`, Fase 46·U2)**: los wrappers animados de `src/components/stats/AnimatedCharts.tsx` (`AnimatedAreaChart`, `AnimatedBarChart`, `AnimatedDonut`) aceptan una prop opcional `label` que aplica `role="img"` + `aria-label` al contenedor del gráfico. Se etiqueta así a los 9 consumidores: `BodyWeightChart` («Evolución del peso corporal»), `VolumeChart` («Volumen de entrenamiento por semana»), `E1rmChart` («Evolución del 1RM estimado»), `ImcChart`, `FrequencyChart`, `RatiosChart`, `CompositionChart`, `SkinfoldChart` y `BodyMeasurementsChart`. Verificado con tsc, build y Playwright 375×812 + 768×1024 (`test_u2.py`, que siembra una DB mínima en IndexedDB para renderizar los charts en `/estadisticas` y `/perfil`).
- **App nativa con Capacitor (`feat`)**: integración de Capacitor 8 (`@capacitor/core/cli/android/ios/app/haptics/splash-screen/status-bar`). `capacitor.config.ts` con `appId com.gymlab.app`, `appName GymLab`, `webDir dist`, fondo `#121214`, splash 800 ms y status bar oscura. `vite.config.ts` usa `base: './'` para que los assets funcionen desde el WebView (`file://`). Plataformas generadas y sincronizadas (`npx cap sync`): `android/` (Gradle, 4 plugins nativos) e `ios/` (proyecto Xcode + `Package.swift`, 4 plugins). APK de debug compilado con éxito (`android/app/build/outputs/apk/debug/app-debug.apk`); `local.properties` apunta al SDK local. iOS listo para abrir en un Mac con Xcode (compilar requiere macOS).
- **Onboarding expandido a 5 pasos (`T1`)**: `src/domain/onboarding.ts` amplía `OnboardingAnswers` (idioma, unidades, sexo, fecha nacimiento, altura, peso, días/semana, duración, cardio, guías, material, términos) con validación de edad 14–99 años (`ageFromBirthDate`, `isBirthDateValid`); `src/domain/settings.ts` añade `measurementSystem: 'metric' | 'imperial'` a `AppSettings`. `src/components/onboarding/Onboarding.tsx` pasa de 3 a 5 pasos con **idioma primero** (Idioma → Objetivo → Semana → Perfil → Resumen): stepper accesible con `aria-current`, transiciones `slideIn`/`slideOut` entre pasos, "Continuar" bloqueado hasta validar cada paso, material contra lista blanca, T&C obligatorio antes de «Empezar D1», guardado en `meta` + sugerencia de rutina como programa activo y sincronización de unidades kg/lb con `AppSettings.units` + `measurementSystem`. `src/components/onboarding/steps.tsx` (nuevo) contiene los 5 pasos con chips ≥44px. Verificado: tsc, build y Playwright en 375×812, 768×1024 y 1024×768 (PASSED).
- **Gráficos mejorados (`T4+T6`)**: refactor de todos los gráficos del app a Recharts animados (in-mount via `useChartEntry` y componentes `AnimatedAreaChart`, `AnimatedBarChart`, `AnimatedDonut`), reemplazando las velas (candlestick) personalizadas por representaciones más claras (barras para volumen, área con puntos para cargas). Gráficos ahora mobile-friendly (taps en lugar de hover, ejes adaptativos, márgenes optimizados, alturas ajustadas) y consistentes con el diseño del sistema. Verificado: tsc, build y Playwright en 375×812, 768×1024 y 1024×768 (PASSED).
- **Tabs internos (`T3`)**: nuevo `src/components/ui/TabNav.tsx` — barra de pestañas con subrayado animado vía anime.js (`translateX` + `width` del indicador, respeta `prefers-reduced-motion`), transición `slideOut`/`slideIn` del contenido con capa saliente absoluta y entrada desde el lado opuesto, navegación por teclado (flechas/Home/End), `role="tablist"/"tab"/"tabpanel"`, `aria-selected`, scroll horizontal si hay muchos tabs, `scrollIntoView` del tab activo y hit ≥44px. Montaje en `/estadisticas` (Entrenamiento · Cuerpo), `/perfil` (Resumen · Historial · Rachas) con nuevo `src/components/profile/RachasSection.tsx` (racha actual/máxima, último entreno y próximo hito de insignia), y días de `/rutinas/:slug` con `src/components/routines/RoutineDayPanel.tsx` extraído del inline. Bug fix: el `useMemo` de `dayTabs` estaba tras el early return `if (!routine)` violando las Rules of Hooks — sustituido por const simple. Verificado: tsc, build y Playwright en 375×812, 768×1024 y 1024×768 (PASSED).
- **Sistema de Logros (`T2`)**: `src/domain/achievements.ts` (dominio puro) define el tipo `Achievement { id, title, description, icon, condition }` y la lista canónica de 8 logros (primer paso, inaugural, racha 7/30 días, primera marca, volumen semanal superado, 50 sesiones, consistencia 4 semanas) con IDs constantes (no input de usuario); `checkAchievements(workouts, streak, prs, sets)` evalúa las condiciones sobre los datos actuales y devuelve solo los que se cumplen. `src/hooks/useAchievements.ts` vigila workouts/PRs/series en Dexie (`useLiveQuery`) y, con debounce de 600 ms para no reaccionar a cada escritura intermedia al guardar una sesión, persiste en `meta.unlockedAchievements` los IDs nuevos **antes** de mostrar el modal (así cada logro se muestra una sola vez) y los acumula para no perder logros simultáneos. `src/components/achievements/AchievementModal.tsx`: modal centrado con backdrop blur, icono con `pulse` en loop (anime.js), `confetti()` al abrir y botón «¡Genial!» con `popScale`; accesible (`role="dialog"`, `aria-modal`, `aria-labelledby`, foco inicial en el botón, cierre con Escape o clic fuera). Se monta globalmente en `src/components/layout/AppShell.tsx` (`AchievementsHost`). Verificado: tsc, build y Playwright en 375×812 y 768×1024 (PASSED).
- **Guías con secciones (`T11`)**: `src/domain/types.ts` añade `GuideSection` (`{ title, content, bullets?: string[] }`) y `sections?: GuideSection[]` a `Guide`. `src/data/seed/guides.ts` añade secciones reales a **todas las guías**: 5 nuevas (técnica de sentadilla, progresión en press de banca, guía para principiantes, recuperación activa, hidratación) y, sobre todo, **desarrolla con explicaciones más amplias las 13 guías que ya existían** (macros, menús de volumen/definición, suplementos, sobrecarga, sueño, mujer, glúteos, HIIT/LISS, estancamiento, deload, espalda segura, sobreentrenamiento) — cada una con 3–4 secciones de «por qué» y «cómo» además de los puntos clave. `src/pages/GuiaDetailPage.tsx` renderiza las secciones como paneles con título `h2` en accent, párrafo y bullets, animación `staggerFade` al entrar; fallback a `keyPoints` si la guía no tiene secciones. Contenido siempre del seed (confiable, sin HTML de usuario). `SEED_VERSION` → `'16'`. Verificado: tsc, build y Playwright en 375×812 y 768×1024 (PASSED).
- **Instrucciones detalladas de ejercicios (`T7`)**: `src/domain/types.ts` añade `ExerciseStep` (`{ step, instruction, tip?, warning? }`) y `detailedSteps?: ExerciseStep[]` a `Exercise`. `src/data/seed/exercises.ts` amplía **17 ejercicios principales** (press banca, dominadas, peso muerto, curl barra, press militar, sentadilla barra, peso muerto rumano, hip thrust, plancha, flexiones, sentadilla búlgara, plancha lateral…) con pasos numerados, tips y avisos de seguridad reales. `src/pages/EjercicioDetailPage.tsx` muestra la técnica como lista numerada (`ol`) con número en círculo, tips con icono `Lightbulb` y warnings con `AlertTriangle` (icono + texto, no solo color), animación `staggerSlide` desde abajo al entrar; si el ejercicio no tiene pasos se mantiene el párrafo `instructions`. **Ampliado a TODOS los ejercicios**: el catálogo ampliado (821 ejercicios) reutiliza solo **31 plantillas de instrucciones** y los ejercicios curados sin pasos tienen instrucción única, así que se derivan los pasos **en render** desde la instrucción — `src/i18n/catalog/exerciseSteps.ts` (`STEPS_BY_INSTRUCTION`, 71 claves ES: 31 plantillas + 40 curados) y `exerciseStepsEn.ts` (`STEPS_BY_INSTRUCTION_EN` en inglés) — aplicados en `localizeExerciseDetail` como overlay (Dexie/seed intactos, sin cambio de `SEED_VERSION`). `test_t7.py` verifica ahora también los pasos derivados de un ejercicio sin `detailedSteps` en seed. Verificado: tsc, build y Playwright en 375×812 y 768×1024 (PASSED).
- **Nombre/alias editable en Perfil (`T5`)**: nuevo hook `src/hooks/useProfileName.ts` que lee/escribe `meta.profileName` vía `metaRepo`. La card del perfil (`src/pages/PerfilPage.tsx`) muestra el alias (o «Atleta» por defecto) y lo edita inline con un botón lápiz «Editar nombre» (≥44px): input `autoFocus` con `maxLength` 24, confirma con Enter o blur y cancela con Escape; el alias persiste y sobrevive a recargas. Verificado: tsc, build y Playwright en 375×812 y 768×1024 (PASSED).
- **Avatar en Perfil (`T5`)**: nuevo hook `src/hooks/useAvatar.ts` que lee/escribe `meta.avatarUri` (URL HTTPS o dataURL) vía `metaRepo`. Nuevo `src/components/profile/AvatarPicker.tsx` (bottom-sheet accesible `role="dialog"`, cierre por Escape/backdrop) con «Subir foto» (`<input type="file" accept="image/jpeg,png,webp,gif">`, `FileReader`, MIME validado + tamaño ≤ 2 MB, se guarda como base64) y galería de 12 avatares predefinidos de hosts conocidos (`images.unsplash.com`, `images.pexels.com`) en grid 4 columnas con scroll (`max-h-[30dvh]`) y `animate-pop` + check al seleccionar. `isSafeAvatarUri()` (exportada) valida que el `src` solo sea `data:image/` válido o HTTPS de host en allowlist. `src/pages/PerfilPage.tsx` muestra el avatar circular con fallback a icono `User` y botón cámara «Cambiar avatar». Verificado: tsc, build y Playwright en 375×812 y 768×1024 (PASSED).
- **Modo grip/lista en el hub Más (`T9`)**: `src/domain/settings.ts` añade `hubLayout: 'grip' | 'list'` a `AppSettings` (por defecto `grip`). `src/pages/MasPage.tsx` incorpora un toggle con `LayoutGrid`/`List` (botones ≥44px, `aria-pressed`, grupo con `aria-labelledby`) que alterna entre rejilla 2 columnas y lista con descripción, ambos con entrada escalonada (`stagger-fade` + delays `.stagger-fade-1..8` en `src/index.css`, nueva clase respetada por `prefers-reduced-motion`). La preferencia se persiste vía `useSettings` y sobrevive a recargas. Verificado: tsc, build y Playwright en 375×812, 768×1024 y 1024×768 (PASSED).
- **Ocultar Papers del hub Más (`T10`)**: se elimina la entrada `Papers` de `src/pages/MasPage.tsx` (con su import `FileText`). La TabBar ya no lo mostraba desde la Fase 42; la sección reaparecerá en la fase social. La ruta `/papers` se conserva en el router y en `useSeo`. Hub con 7 entradas sin huecos; filas táctiles ≥44px intactas. Verificado: tsc, build y Playwright en 375×812 y 768×1024 (PASSED).
- **Setup anime.js + helpers de animación (`F43`)**: se instala `animejs@3` + `@types/animejs` y nace `src/lib/animations.ts` con helpers reutilizables `fadeIn`/`fadeOut`, `slideIn`/`slideOut` (dirección left/right/up/down), `staggerFade`/`staggerSlide` (con `anime.stagger`), `popScale`, `pulse`, `drawOn` (SVG vía `stroke-dashoffset`), `confetti` (partículas con translate/rotate/scale/color aleatorios) y `ensureVisible`. Todos respetan `prefers-reduced-motion` aplicando el estado final sin animar (nada queda invisible). Nueva clase `.anime-ready { opacity: 0 }` en `src/index.css` como estado base que las animaciones controlan. Se añade infraestructura de verificación dual **Playwright**: `scripts/with_server.py` (levanta/reutiliza el dev server en el puerto `E2E_PORT` y ejecuta el `test_*.py`, con limpieza del árbol de procesos en Windows) y `scripts/e2e_utils.py` (viewports iPhone 375×812, iPad 768×1024 y iPad landscape 1024×768, screenshots con sufijo por vista, captura de errores de consola/pageerror). Primer test `test_f43.py` que monta una página de prueba (`public/test-animations.html`) y verifica fadeIn/slideIn/popScale visibles, `drawOn` dejando `stroke-dashoffset` en 0 y confetti con ≥20 partículas, más reduced-motion sin animar. Verificado: `npx tsc --noEmit`, `npm run build`, `npm run lint` y Playwright en 375×812 y 768×1024 (PASSED).
- **Verificación dual móvil + tablet (`docs`)**: el criterio de hecho de la Fase 43 en `PLAN.md` ahora exige Playwright en **iPhone 375×812** e **iPad 768×1024** para todas las tareas, más **iPad landscape 1024×768** para las de layout ancho (T9, T3, T4+T6, T1). Cada tarea añade su subtarea de verificación y sigue siendo mobile-first (touch ≥44px, thumb-zone, `safe-area`, drag-scroll, `prefers-reduced-motion`).

### Changed
- **Template literal sin interpolación en `AvatarPicker.tsx:146` (`chore`, Fase 46·U3)**: el `aria-label` «Usar avatar predefinido» pasa de `` `...` `` a string plano (sin cambios de comportamiento).
- **Placeholders con contraste AA (`fix`)**: los `placeholder` dejan de usar `text-muted/70` (≈4.4:1, bajo AA) y pasan a `text-muted` (6.8:1) en los inputs de la app (calculadoras, set rows, búsquedas, notas, peso corporal, builder de rutinas).- **Botones «?» como popover en la esquina superior derecha (`craft pass`)**: el componente `InfoTip` (`src/components/ui/InfoTip.tsx`) deja de expandir el texto inline y abre un popover flotante (tooltip anclado bajo el botón, `role="dialog"`, cierre con clic fuera o Escape) sobre `bg-bg-elevated` sólido para legibilidad. En el insight de volumen (`InsightCard`) y en el panel de deload (`EntrenarPage`) el botón pasa de estar al final del texto a la esquina superior derecha de la tarjeta, alineado con el título (`justify-between`), manteniendo touch ≥ 44px.

### Added
- **`ConfirmSheet`, hoja de confirmación del tema (`feat`)**: nuevo `src/components/ui/ConfirmSheet.tsx` que sustituye a `window.confirm` por un bottom-sheet con `panel-floating`, `role="alertdialog"` y cierre por Escape/backdrop. Se aplica en la sesión activa (salir sin guardar y aviso de series sin peso en `EntrenamientoPage`), al eliminar una rutina propia (`RutinaDetailPage`) y al restaurar un backup (`AjustesPage`, que ahora deja la importación pendiente hasta confirmar). Nueva variante `danger` en `src/components/ui/Button.tsx` (fondo `--color-danger`) para acciones destructivas.
- **Buscador textual en `/rutinas` (`feat`)**: campo de búsqueda con icono que filtra por título sobre favoritas, propias y predefinidas, combinable con los filtros de objetivo/nivel/tipo; estado vacío «No hay rutinas que coincidan con «query»».
- **Botón «?» por zona en medidas y grasa corporal (`feat`)**: cada campo de `MedidasCorporalesPage` y `GrasaCorporalPage` tiene ahora su propio `InfoTip` (popover) con la técnica de medición de esa zona (`zone.guide`/`site.guide`). Se eliminan las guías desplegables «Cómo medir»/«Cómo usar el picómetro» (`MeasurementsGuide`, `SkinfoldGuide`) y sus textos de técnica (`MEASUREMENT_TIPS`, `SKINFOLD_TECHNIQUE`) por redundantes; las explicaciones «Para qué registrar medidas» y «Cómo se calcula el % de grasa» se mueven a la cabecera del panel «Registrar hoy».
- **`InfoTip` con posicionamiento dentro del viewport (`feat`)**: el popover pasa a `position: fixed` con coordenadas calculadas (top/left) que se recalcular al hacer scroll/resize, abriendo hacia abajo o arriba y a izquierda/derecha según el espacio disponible, para no cortarse nunca. Requiere que `animate-page-in` use `animation-fill-mode: backwards` (no `both`) en `src/index.css`: el `transform: translateY(0)` residual tras la animación convertía al `<main>` en containing block y descolocaba los popovers `fixed` al hacer scroll.
- **Instrucción de scroll en `AGENTS.md` (`docs`)**: se documenta en la sección «Diseño y UX» que ningún scrollbar debe ser visible en ninguna página y que todo lo que haga scroll (listas, carruseles, contenedores) debe poder desplazarse arrastrando con el dedo/ratón (modo «drag-scrolling» de `body`, ya implementado en `src/index.css`).
- **Fotos para rutinas custom (`feat`)**: las rutinas creadas por el usuario (sin `imageUrl`) dejan de repetir `default.jpg` y muestran una foto del catálogo elegida por `id` de forma determinista (`fallbackImages[routine.id % pool]`, pool derivado de las `imageUrl` de las predefinidas en `src/pages/RutinasPage.tsx`), evitando imágenes repetidas entre rutinas custom y manteniéndose estable entre renders.
- **Contraste reforzado en tarjetas de rutina (`fix`)**: `.routine-card::before` (`src/index.css`) aplica ahora un velo centrado en el 60% del color de fondo (66→60→54% horizontal y 62→56→50% vertical) y `.routine-card__content` gana un halo doble (`text-shadow` de 3px + blur 14px) con el color de fondo del tema (oscuro en modo noche, claro en modo día), mejorando la legibilidad del título y metadatos sobre la foto en cualquier paleta × tema. **Causa raíz añadida**: el velo (`::before`) y el tinte (`::after`) se pintaban **detrás** de la imagen porque `img`, `::before` y `::after` compartían `z-index` auto (orden de DOM: imagen por encima del primer pseudoelemento). Se fija la jerarquía con `z-index` explícito — imagen `0`, velo `1`, tinte `2`, enlace `3` — tanto en `.routine-card` como en `.hero-atmosphere` (mismo bug en el hero de Home). Verificado por pixelado de pantalla: la zona de imagen pura pasa a un gris oscuro del fondo (38,38,36).
- **Fotos de rutinas (`feat`)**: cada rutina del catálogo (`src/data/seed/routines.ts`) gana `imageUrl` apuntando a una foto real (Pexels, licencia libre) en `public/images/routines/<slug>.jpg` (24 ficheros, `w=1200`, la PNG se sirve como JPEG). Campo opcional `imageUrl` en el tipo `Routine`; `SEED_VERSION` sube a `'13'` para re-sembrar el catálogo preservando rutinas custom. La tarjeta de `/rutinas` pasa de fila con icono a tarjeta con foto de fondo (`.routine-card` en `src/index.css`): imagen cubriendo, velo de legibilidad con gradientes de `--color-bg` y tinte del CTA `mix-blend-mode: soft-light` — todo con variables CSS para adaptarse a las 6 paletas × 2 temas (en modo día la foto baja a `opacity 0.5`), chip de objetivo tipo cristal con `backdrop-filter` y botón favorito con fondo sutil. El hero de Home (`EntrenarPage`) muestra la foto de la **rutina activa** (vía `useActiveProgram`); rutina custom sin foto → `default.jpg`; **sin rutina activa se conserva `home-hero.jpg`** como hasta ahora. Sin cambios en la página de detalle. Verificado con typecheck y build (las imágenes entran en el precache PWA).
- **Atmósfera fotográfica en el hero de Home (`feat`)**: nueva capa decorativa `.hero-atmosphere` (`src/index.css`) que coloca una foto de gimnasio oscuro (`public/images/home-hero.jpg`, 1600×900, Pexels · licencia libre, fuente anotada en `.interface-design/system.md`) detrás del `panel-hero` de `EntrenarPage` (líneas 133-137), con `object-fit: cover` a `opacity 0.5`, velo del fondo en degradado a izquierda/abajo (mantiene contraste del texto) y tinte duotono del CTA con `mix-blend-mode: soft-light`; en modo día la foto baja a `opacity 0.16`. Capa `aria-hidden` + `pointer-events: none`, contenido envuelto en `relative z-10`. Es la única superficie fotográfica de la app (regla de escasez documentada en `DESIGN.md` → componente firma «Hero Atmosphere», `.impeccable/design.json` y `system.md`). Verificado con typecheck y build.
- **Componente `Button`/`ButtonLink` del sistema (`craft pass`)**: nuevo `src/components/ui/Button.tsx` con variantes `primary` (CTA dorado `gold-gradient`), `outline`, `accent` (secundario destacado, usado en el backup) y `ghost`, más tamaños `sm`/`md`/`lg` con touch ≥ 44px. Sustituye las cadenas `gold-gradient flex min-h-*...` repetidas en 11 archivos (Entrenar, Sesión, RestTimer, Onboarding, Rutinas, Detalle/Builder de rutina, Peso corporal, InstallBanner y Ajustes), eliminando la duplicación de estilos.
- **`panel-floating` (nivel 2 de elevación)**: nueva clase en `src/index.css` para overlays/sheets (borde dorado más presente, gradiente más claro y sombra mayor). Se aplica a la calculadora de discos (`PlateCalculatorModal`), que antes usaba un fondo plano `bg-bg-elevated`.
- **Indicador de pestaña activa en la tab bar**: el `TabBar` muestra ahora una píldora `bg-cta/15` detrás del icono de la pestaña activa, haciendo inconfundible «dónde estás» sin ruido visual; el icono activo crece levemente (`scale-105`) y el inactivo responde al pulsarlo (`scale-95`).
- **Sistema de diseño documentado (`.interface-design/system.md`)**: dirección industrial-premium, estrategia de profundidad (gradiente + highlight + borde sutil), escala de elevación `panel`/`panel-floating`/`panel-hero`, jerarquía tipográfica y medidas de los componentes clave (`Button`/`ButtonLink`, `BackLink`, `ProgressRing`, `Toggle`, inputs) para que futuras sesiones las respeten.
- **Instrucción de planificación en `AGENTS.md`**: sección «Planear antes de implementar (obligatorio)» que obliga a usar la skill **brainstorming** (explorar contexto → clarificar → 2-3 enfoques → diseño aprobado → spec en `docs/superpowers/specs/` → commit) antes de escribir código de cualquier feature o cambio de comportamiento.

### Changed
- **Un solo foco por vista en Home (`craft pass`)**: se elimina el botón flotante inferior duplicado («Iniciar entrenamiento» / «Continuar entreno») de `/`; queda el CTA del hero como único foco de la tarjeta «Hoy toca».
- **Progreso de programa/sesión como barra en Home**: el segundo `ProgressRing` (que duplicaba el del hero) se sustituye por una barra de progreso compacta `gold-gradient` con `role="progressbar"`, diferenciando la expresión y dando variedad a las métricas.
- **Bordes de superficie más sutiles (`craft pass`)**: el borde dorado de `.panel` baja de 35% a 20% de opacidad y el de `.panel-hero` de 55% a 45%, para que la jerarquía venga del cambio tonal y no del contorno. Se añade `.panel-floating` como nivel de elevación superior.
- **Backup con componente Button**: en Ajustes, «Exportar backup» usa la variante `accent` y «Restaurar desde archivo» la variante `outline`, manteniendo el `input[type=file]` oculto accesible.

### Fixed
- **Inputs de series/reps/descanso en el builder de rutinas (`fix`)**: al borrar el valor, el campo revertía a 1 en el acto (`clamp(Number(e.target.value) || 1, ...)`) haciendo imposible teclear el valor real. Nuevo componente `TargetInput` en `RutinaBuilderPage.tsx` con draft local en string: el campo puede quedarse vacío mientras se escribe y valida (clamp + mínimo) en `blur`. Además `targetSets` sube su máximo de 20 a 99 (reps ya admitía 100). Verificado con `npx tsc --noEmit`.
- **Test de rendimiento de navegación (`perf`)**: nuevo `test_perf.py` (Playwright, vía `scripts/with_server.py`) que mide cold load de la home (LCP/FCP/TTFB/DOMContentLoaded + tamaño JS inicial, contexto sin cache) y la navegación SPA warm por las 10 rutas principales (transición desde el click hasta el heading visible, suma de Long Tasks que bloquean el hilo y chunks lazy descargados). Umbrales: LCP ≤2500 ms, FCP ≤2000 ms, TTFB ≤800 ms, DCL ≤2000 ms, navegación SPA ≤700 ms y ≤500 ms de long tasks. Resultado actual: LCP 460 ms, FCP 240 ms, TTFB 16 ms, DCL 210 ms y navegación SPA de 14–63 ms en todas las rutas con 0 long tasks. Nota: los links del hub Más se clickean con `force=True` porque el wait de estabilidad de Playwright incluiría la animación de entrada del hub (staggerFade), no la transición real.
- **Touch targets ≥44px en botones solo-icono (`fix`, Fase 46·U1)**: los botones de 40px pasan a área efectiva de pulsación ≥44px con el patrón `after:-inset-1` (hit-area ampliado sin cambiar el tamaño visible, ya usado en `RutinasPage`): favorito en `EjerciciosPage` y `ExercisePicker`, cerrar en `ExercisePicker`, `PlateCalculatorModal` y `ConfirmSheet`, y descartar en `InstallBanner`. El toggle kg/lb de `ConversorPage` pasa de `h-10` a `min-h-[44px]`. Verificado con tsc, lint, build y Playwright 375×812 + 768×1024 (`test_u1.py`: hit-area efectivo midiendo el inset del `::after`).
- **Pantalla en blanco al terminar el onboarding (`fix`)**: en `src/components/onboarding/Onboarding.tsx` el `useEffect` de la transición `slideIn` entre pasos estaba después del early return `if (done || workouts.length > 0) return null`, violando las Rules of Hooks: al completar el wizard (`done` → true) React fallaba con «Rendered fewer hooks than expected» y el home no cargaba. Se mueve el `useEffect` antes del early return. Verificado con tsc, build y Playwright (flujo completo 5 pasos → home con programa activo «PPL Volumen», y skip «Ya entreno aquí» → home sin programa; 0 errores de consola).
- **Touch targets ≥ 44px (`fix`, auditoría impeccable)**: se suben a `min-h-[44px]` todos los controles interactivos por debajo de la regla de AGENTS.md: chips de filtro de `/rutinas` y `/papers`, selector de día en detalle de rutina, toggle de sexo en Calorías (TDEE), presets del `RestTimer`, pills de `ExercisePills`/`BodyMeasurementsChart`, botones «Solo favoritos» (`ExercisePicker`), recientes de calculadoras, «Calculadora de discos», links de PubMed, «Limpiar filtros», skip del onboarding, `BackLink` de historial, botón «Deshacer» del `UndoToast`, celdas compactas del `MonthCalendar` y botón «Sugerido» de `ExerciseBlock`.
- **Popover de InfoTip cortado en el borde de pantalla (`fix`)**: el popover se anclaba a la izquierda del botón `?` y abría hacia la derecha, saliéndose por el borde cuando el botón está en la esquina superior derecha. Ahora se ancla a la derecha del botón (`right-0`) y abre hacia dentro; verificado en viewports de 390px y 360px (dentro de pantalla).
- **Scroll por arrastre roto en scroll de página (`fix`)**: `useGlobalDragScroll` no desplazaba la página porque el bucle buscaba el contenedor scrollable antes de llegar a `<html>` y detectaba falsos positivos (elementos con `overflow: visible` cuyo contenido se desborda, donde `scrollTop` no hace nada). Ahora `isScrollable` exige `overflow` auto/scroll/overlay con contenido sobrante, o `<html>` (scroll de documento), y sube hasta el ancestro real; el arrastre de la página y de carruseles/listas funciona (verificado: 300px de scroll con el gesto).
- **Botón favorito pegado al borde derecho de la tarjeta (`fix`)**: en `src/pages/RutinasPage.tsx` el botón pasa de `size-11` a `size-10` con `mr-1.5`, dejando 7px de respiro respecto al borde; conserva el área táctil ≥ 44px (regla de AGENTS.md) mediante un pseudo-elemento `after:-inset-1` invisible.
- **Fake data para probar la app**: generador determinista `scripts/generateFakeData.cjs` (Node puro, sin dependencias) que produce `fake-data.json` en el formato de backup de GymLab (importable desde Ajustes → Importar datos). Cubre todas las tablas de usuario con datos coherentes: 60 entrenamientos PPL (rutina seed `1`) de abr a ago 2026 (L/X/V, +J/S en las 2 últimas semanas) con 1209 series, `totalVolume` calculado con la misma suma que la app y horas de inicio/fin y de serie válidas (sin `NaN` en duraciones); 15 PRs con e1RM Brzycki consistente con los sets; peso corporal diario (125 registros, tendencia 84→78 kg); medidas corporales y pliegues cutáneos mensuales (Jackson-Pollock 7 sitios); perfil con `weeklyGoal: 3`; programa activo (L/X/V); `meta` (settings con RPE/RIR activados, `onboardingDone`, favoritos/recientes de ejercicios y rutinas, altura/sexo, tema noche + paleta dorada); notas por ejercicio y stubs sociales (perfil, 3 posts, 1 media). Las tablas de catálogo/seed se omiten a propósito para que el import preserve las rutinas y ejercicios reales. Verificado con typecheck/build, una validación de consistencia (volumen, PRs, unicidad de ids) y Playwright (import + 40/40 checks de UI: home, estadísticas, perfil, fatiga muscular, rutinas, ejercicios, ficha, calendario, medidas, grasa, peso y detalle de sesión; sin errores de consola).
- **Documentación de todo el código (`docs`)**: comentarios en español a nivel de bloque/función en los ~180 archivos fuente (`src/domain`, `src/domain/calculators`, `src/data`, `src/hooks`, `src/store`, `src/lib`, `src/app`, `src/components`, `src/pages`, `scripts/genCatalog.cjs`, `vite.config.ts`, `index.html` y `src/index.css`). Cada archivo lleva una cabecera breve con su propósito y comentarios puntuales sobre la lógica no trivial (fórmulas, unidades kg/lb, selectores de store, timers/sonido, upserts por fecha, paletas×tema), sin cambios de comportamiento. Verificado con `npx tsc --noEmit`, `npm run build` y `npm run lint`.
- **Tab «Estadísticas» (`Fase 42`)**: nuevo 4º tab en la barra inferior (Entrenar · Rutinas · Estadísticas · Más) con ruta `/estadisticas` (`src/pages/EstadisticasPage.tsx`), que agrega rendimiento y composición corporal con **8 tipos de gráfico**. Sección Entrenamiento: 6 KPIs en texto (racha actual/máxima, entrenos 30 d, duración media, volumen semanal, total), bullet «Objetivo semanal» vs `profile.weeklyGoal` (anillo de progreso + cuenta 2/3), volumen por semana (área), frecuencia semanal (barras con valores), volumen por grupo muscular (barras horizontales + donut de reparto), **velas de cargas por sesión** por ejercicio (OHLC: apertura/cierre = 1ª/última serie, alto/bajo máx-mín, sin calentamientos, con selector de ejercicio en pills) y **velas de rango de volumen semanal**, y fuerza estimada e1RM por ejercicio (línea). Sección Cuerpo: peso, IMC (línea con categoría en el tooltip), medidas por zona, ratios WHtR/WHR (líneas con `ReferenceLine` de umbral por sexo), % grasa (línea) y composición (línea masa grasa/magra + donut % grasa vs magra + categoría). Estado vacío con CTA a Entrenar; a11y con `role="img"`/`aria-label`, leyendas y valores como texto. Dominio puro en `src/domain/trainingStats.ts` (frecuencia, duración media, días entrenados, racha máx, volumen por músculo, velas de cargas/volumen, progreso de objetivo) y series ampliadas en `src/domain/calculators/bodyComposition.ts` (IMC, ratios, composición), con 33 tests nuevos. Candlestick = `ComposedChart` + `Bar` con `shape` SVG propio (`CandlestickChart.tsx`).
- **Hook `useMetaValue` y backup de tablas corporales (`Fase 42`, Apartado B)**: nuevo `src/hooks/useMetaValue.ts` que envuelve `useLiveQuery` para leer JSON de `meta` reactivamente; `MedidasCorporalesPage` y `GrasaCorporalPage` dejan de usar `useLiveQuery` directamente. `src/data/backup.ts` incluye ahora `bodyMeasurements` y `skinfolds` en `ALL_TABLES` (export/import no perdía las medidas ni los pliegues). Verificado con typecheck, build, 65 tests y Playwright (render de todos los tipos de SVG, tab activo con `aria-current`, pills activas, tooltip de velas, 0 errores de consola).
- **Medidas corporales y grasa corporal dentro de Calculadoras (`Fase 41`, revisión)**: `Medidas corporales` y `Grasa corporal` se mueven del hub Más a `/calculadoras/medidas` y `/calculadoras/grasa`, con sus tarjetas (iconos Ruler y Percent) en el grid de calculadoras y actualización de rutas, back-links y descripciones SEO. Se elimina la redundancia de explicaciones: la guía paso a paso única y desplegable («Cómo medir» y «Cómo usar el picómetro») queda como única fuente de instrucciones, quitando los botones «?» por zona/pliegue que duplicaban el mismo texto. Correcciones de ortografía/gramática en guías y textos (p. ej. «No sustituye» → «No sustituyen» en el pie de /medidas, y pulido de la técnica del picómetro).
- **Medidas corporales (`Fase 41`)**: calculadora `/calculadoras/medidas` para registrar por fecha 18 zonas corporales en cm (tronco, brazos y piernas), agrupadas y con guía desplegable paso a paso: técnica de la cinta (6 pasos) e instrucción de medición por cada zona. Guardado con merge parcial por zona sobre la entrada del día; altura y sexo se guardan una vez en `meta` (`heightCm`, `bodySex`). Muestra última medición con delta vs anterior, ratios WHtR («Saludable»/«Riesgo aumentado»/«Riesgo alto»), WHR («Riesgo bajo»/«Riesgo moderado»/«Riesgo alto») y simetría izq-der de las 6 zonas pareadas, más gráfico de evolución con selector de zona y rango 30/90/todo. Catálogos y guías en `src/domain/bodyMeasurements.ts`.
- **Grasa corporal con picómetro (`Fase 41`)**: calculadora `/calculadoras/grasa` que calcula el % de grasa en vivo mediante Jackson-Pollock (7 pliegues, o 3: pectoral+abdominal+muslo en hombres y tríceps+suprailíaco+muslo en mujeres) y la ecuación de Siri (densidad → % grasa), con categoría por sexo (Esencial/Atleta/En forma/Promedio/Alto) y masa grasa/magra si se indica peso. Guía desplegable con técnica de la pinza paso a paso y punto exacto de cada uno de los 7 pliegues. Guarda por fecha (sexo, edad, peso opcional y pliegues en mm), con «Último registro» e historial con gráfico de evolución. Lógica en `src/domain/calculators/bodyComposition.ts` con 19 tests nuevos.
- **Persistencia Dexie v4 (`Fase 41`)**: nuevas tablas `bodyMeasurements` y `skinfolds` (key `id, localDate`) con repos `bodyMeasurementRepo` (upsert con merge parcial) y `skinfoldRepo`, hooks `useBodyMeasurements` y `useSkinfolds`, tarjetas en el hub de calculadoras, rutas lazy `/calculadoras/medidas` y `/calculadoras/grasa` y descripciones SEO en `useSeo.ts`. Verificado con typecheck, build, 46 tests y Playwright (11/11 checks, sin errores de consola).
- **Alarma de cuenta atrás por segundo (`fix`, RestTimer)**: cuando quedan ≤3 s de descanso suena un pitido de aviso en cada uno de los últimos 3 segundos (3, 2 y 1), en vez de uno solo; `playRestWarningSound` se dispara por cada valor distinto de `restRemaining` (guard `lastWarnedRef` en vez del antiguo `warnedRef` que bloqueaba el resto de pitidos), solo si `settings.restSound` está activo.
- **Ring de boxeo al completar serie**: marcar una serie como completada en la sesión (`handleSetCompleted`) reproduce el ring de campana de boxeo (`playBoxingBellSound`) en lugar del pitido corto (`playSetCompleteSound`), independientemente de `settings.restSound`.
- **Feedback sonoro de sesión y descanso**: al iniciar un entreno suena un "ring" de campana de boxeo (dos golpes metálicos sintetizados con WebAudio en `playBoxingBellSound`, `src/lib/feedback.ts`) — suena en `startWorkout`, al reproducir un día de rutina (`loadRoutineDay`) y al añadir el primer ejercicio de una sesión vacía (que ahora también fija `startedAt`, antes quedaba en null y la duración se guardaba como 0). El `RestTimer` avisa con un pitido corto (`playRestWarningSound`, 523 Hz) cuando quedan ≤3 s de descanso, solo si `settings.restSound` está activo, y mantiene el sonido de fin al llegar a 0.
- **Reloj vivo del tiempo de sesión**: el stat "Tiempo" del header de `/entrenamiento/active` pasa de mostrar minutos estáticos a un cronómetro que tic-tac cada segundo en formato `mm:ss` (`h:mm:ss` si supera la hora) con `tabular-nums`. Nuevo helper `formatElapsedClock` en `src/domain/workouts.ts`. Verificado con Playwright (9/9 checks, sin errores de consola).
- **Cierre de Fase 31 (pasadas de `mobile-app-ui-design`)**: `RestTimer` muestra la afirmación «Vuelve a por la siguiente» cuando la cuenta llega a 0 (`aria-live="polite"`); `ExercisePicker` maneja estados de carga/error (`useExerciseCatalog` expone `loading`; skeleton con `role="status"` mientras carga y fallback `role="alert"` si el catálogo no se pudo cargar); `/calculadoras` gana barra de búsqueda instantánea (`Smarter Search`), sección «Recientes» en localStorage (`gymlab.recentCalculators`, máx. 3) y estado vacío «No hay calculadoras que coincidan», con la tarjeta «Calculadora de discos» a ancho completo. Verificado con Playwright (8/8 checks, sin errores de consola).
- **SEO por ruta (`lote C`)**: nuevo hook `src/hooks/useSeo.ts` montado en `AppHeader` que, según `pathname`, actualiza `document.title`, `meta[name=description]` (150–160 chars por página), tags OG (`og:title/description/url/image/site_name`), Twitter cards y el `<link rel="canonical">` con la URL absoluta (base `https://gymlab.app/`, constante editable). Cubre 23 rutas (rutinas, papers, guías, calculadoras, ejercicios, sesión, calendario, cuerpo, peso, perfil, ajustes) con fallback a una descripción genérica. `index.html` gana title descriptivo ("GymLab — Entrena mejor con datos"), meta description ampliada, `meta[name=robots]`, canonical y tags OG/Twitter con `logo.jpg` como imagen. Verificado con Playwright (title/description/canonical/OG por ruta).
- **PWA installable (`lote C`)**: nuevos iconos PNG a partir de `public/logo.jpg` — `icon-192.png`, `icon-512.png` y `icon-maskable-512.png` (512×512 con logo centrado en el safe zone sobre `#121214`). `vite.config.ts` referencia los PNG (any/maskable) y añade `id: '/'` al manifest, cumpliendo los requisitos de instalación de Chrome/Android.
- **Favoritos de rutinas (`feat`)**: botón estrella en las tarjetas de `/rutinas` y en el detalle (`src/hooks/useRoutineFavorites.ts`, clave `routineFavorites` en `metaRepo`, mismo patrón que `useExerciseFavorites`); sección «Favoritas» en la lista.
- **Rutina activa destacada (`feat`)**: la tarjeta de la rutina del programa activo muestra el badge «Activa» con borde CTA en `/rutinas`.
- **Mínimo de días según la rutina (`feat`)**: al seguir una rutina se exigen al menos `daysCount` días de la semana (p. ej. 5 para una de 5 días), con la pista «Selecciona al menos N días» y `aria-pressed` en los botones de día.
- **Duración estimada con +1 min por serie (`feat`)**: `estimateWorkoutMinutes` (`src/domain/calendar.ts`) suma 1 minuto por serie a la estimación del detalle de rutina.
- **Scrollbars ocultas y scroll por arrastre (`feat`)**: las scrollbars de todos los elementos se ocultan (`scrollbar-width: none`) y el contenido se arrastra con ratón o dedo — nuevo `src/hooks/useGlobalDragScroll.ts` (ignora botones, enlaces, inputs y `[data-no-drag]`, umbral 4 px, cursor `grab`/`grabbing`) montado en `src/App.tsx`.
- **Días preseleccionados al seguir una rutina (`feat`)**: la sección «Seguir programa · días» de `/rutinas/:slug` arranca con los días ya marcados según la rutina (p. ej. L-X-V para 3 días, L a V para 5), o con los días del programa activo si la rutina ya se sigue («actualizar días»); el botón «Seguir esta rutina» queda habilitado sin tocar nada. Verificado con Playwright (5 días → 5 marcados, 3 días → 3 marcados, sin pista de mínimo).
- **Paleta de escala de grises (`feat`)**: nueva paleta «Gris» en Ajustes → Apariencia (junto a Dorado, Energía, Carmesí, Eléctrico y Violeta). Al ser una paleta, funciona con los dos temas de fondo: noche (`--color-bg` oscuro, acentos plata `#b9b9bf`) y día (`#fafafa`, acentos `#6b6b70`), con semánticos desaturados. Se persiste como `gymlab.palette` y los gráficos la respetan vía `useThemeColors`. Los temas grises de la versión anterior (`gymlab.theme='gray-night'` / `'gray-day'`) migran automáticamente a paleta `gray` con el tema correspondiente. Verificado con Playwright (paleta gris en noche y día, sin errores de consola).

### Changed
- **Cabecera sin logo ni marca (`provisional`)**: `AppHeader` deja de mostrar la fila del logo y el texto «GymLab»; solo queda el título/subtítulo de la página. Cambio provisional para probar el look — se puede revertir.
- **Papers fuera de la tab bar (`feat`)**: el tab «Papers» se retira de la barra inferior (ahora Entrenar / Rutinas / Más) y se añade como entrada del hub Más («Papers — Biblioteca de artículos y referencias»). La ruta `/papers` sigue operativa; los back-links del detalle no cambian.
- **Queries de datos en hooks de dominio (`refactor`, lote D)**: los 14 usos directos de `useLiveQuery` (`dexie-react-hooks`) que había en pages/components se envuelven en hooks de dominio en `src/hooks/` — `useActiveProgram` (programa activo + su rutina), `useRoutines` (`useRoutines`, `useRoutineSlugs`, `useRoutineDays`, `useRoutineDetail` con días e items enriquecidos, `useRoutineDayMuscleGroups`), `usePapers`/`usePaperBySlug`, `useGuides`/`useGuideBySlug`, `useWorkoutSets` y `useExerciseDetail` (ejercicio por slug + últimos sets + histórico e1RM) — y se reutilizan hooks existentes (`useWorkouts`, `useExerciseCatalog`, `useWorkout`, `useStreak`). Refactorizadas: `EntrenarPage`, `PerfilPage`, `CalendarioPage`, `CuerpoPage`, `RutinasPage`, `RutinaDetailPage`, `RutinaBuilderPage`, `PapersPage`, `PaperDetailPage`, `GuiasPage`, `GuiaDetailPage`, `EjercicioDetailPage`, `Onboarding` y `WorkoutDetail`. Ya no queda `useLiveQuery` fuera de `src/hooks/`. Verificado con typecheck, build, lint (sin warnings nuevos) y Playwright (15 checks, 15/15, sin errores de consola nuevos).
- **Diseño consistente industrial-premium (lote B de la Fase 39)**: los tokens `.panel` / `.panel-hero` / `.kicker` / `.stat-value` / `.chip` (antes solo en Home y Sesión) se aplican en toda la app: Rutinas, Detalle de rutina, Papers, Guías, Ejercicios, Cuerpo, Calendario, Perfil, Ajustes, Peso corporal, hub "Más" y las 6 calculadoras (badges/captions/estadísticas y superficies pasan a los tokens). Los emojis usados como iconos de grupos musculares se sustituyen por iconos lucide (`src/components/exercises/MuscleGroupIcon.tsx`, reutilizado en `EjerciciosPage`, `ExerciseFilterBar` y `ExercisePicker`; se elimina `muscleGroupEmoji` de `useExerciseCatalog`). Colores hardcodeados → tokens CSS: categorías de IMC (`imc.ts` → `var(--color-*)`), `text-blue-400` → `text-info` (`routineMeta.ts`) y los swatches de paleta de Ajustes → clases `.swatch-*`. Nuevo token `--color-info` (noche `#3b82f6` / día `#1d4ed8`) con su variante para la paleta. **Back consistente**: nuevo `src/components/ui/BackLink.tsx` (label "Volver", min 44px) añadido a Perfil, Ajustes y Calculadoras (`/mas`), y aplicado a 20+ pantallas (calendario, cuerpo, guías, ejercicios, peso, 6 calculadoras, rutinas, detalle de rutina, builder, ficha de ejercicio, guía, paper, sesión y detalle de workout) con labels contextuales donde aplica. Verificado con typecheck, build, 27 tests y Playwright (DOM: tokens aplicados, sin emojis, sin errores de consola).

### Fixed
- **Paleta guardada al refrescar (`fix`)**: el script inline de `index.html` que aplica el tema al cargar validaba la paleta contra una lista sin `'gray'`, así que al recargar con la paleta gris guardada caía en el fallback dorado hasta entrar en Ajustes. Se añade `'gray'` a la lista. Verificado con Playwright (reload con `gymlab.palette='gray'` → `data-palette=gray`, cta plata).
- **Sesión desde Entrenar arranca con los ejercicios del día (`fix`)**: `handleStart` de `EntrenarPage` precarga el día programado del programa activo mediante el nuevo hook `useRoutineDayItems` (`src/hooks/useRoutines.ts`) + `startRoutineDay`, en vez de iniciar la sesión en blanco y obligar a añadir cada ejercicio a mano; sin día programado sigue arrancando sesión vacía.
- **Ring de boxeo al terminar el descanso (`fix`)**: el `RestTimer` reproduce `playBoxingBellSound` cuando la cuenta llega a 0 (antes sonaba el triple pitido `playRestEndSound`, que se conserva exportado en `src/lib/feedback.ts` para otros usos); solo si `settings.restSound` está activo.
- **Pitidos finales del descanso audibles (`fix`)**: `beep` de `src/lib/feedback.ts` gana el parámetro `vol` y `playRestWarningSound` pasa de 523 Hz (vol 0.18, casi inaudible) a 880 Hz con vol 0.4; los avisos de los últimos 3 s del `RestTimer` ahora se perciben con claridad.
- **Rendimiento de la sesión (`optimize`)**: el cronómetro de `/entrenamiento/active` se aísla en un nuevo componente `ElapsedClock` (`src/components/workout/ElapsedClock.tsx`) que maneja su propio `setInterval`, de modo que el tick de 1 s ya no re-renderiza `EntrenamientoPage` completa ni sus `ExerciseBlock`/gráficos. Además, las suscripciones al store de sesión (`activeWorkoutStore`) pasan de selector global a selectores individuales en `EntrenamientoPage`, `EntrenarPage`, `RutinaDetailPage`, `ExerciseBlock` y `RestTimer`, evitando re-renderizados por cada tick de `restRemaining`.
- **RestTimer no terminaba el descanso solo (`fix`)**: el intervalo se limpiaba al llegar `restRemaining` a 0 y nunca se ejecutaba el tick final que pone `isResting=false` (`tickRest` solo lo llama este componente), dejando la sesión en «OK» con «Pausar» hasta pulsarlo a mano. Ahora el intervalo corre mientras `isResting` y `tickRest` lo cierra al llegar a 0. De paso, el sonido/vibración de fin de descanso y la afirmación «Vuelve a por la siguiente» solo se disparan cuando la cuenta llega realmente a 0 (antes el sonido también sonaba al montar la página o al pausar).
- **Bugs menores de UX** (`lote E`): en `EntrenamientoPage` se corrige la clase inválida `min-h-100dvh` → `min-h-dvh` en el resumen de sesión, y el `scrollIntoView` automático al completar un grupo usa `behavior: 'auto'` cuando el sistema tiene `prefers-reduced-motion: reduce` (antes siempre `smooth`). En `RutinaDetailPage`, el botón «Seguir esta rutina» refleja el estado real del programa activo: si la rutina ya es la activa muestra «Rutina activa · actualizar días» con icono de check (sigue permitiendo ajustar los días). En `PapersPage` se elimina el `<a>` anidado (enlace PubMed dentro del `<Link>` de la tarjeta) reestructurando la tarjeta: el `<Link>` envuelve el contenido y el enlace externo queda como hermano — desaparece el warning de React "a cannot be a descendant of a" en consola. Verificado con Playwright (3/3 checks del estado de rutina activa + `NO_CONSOLE_ERRORS`).
- **Validación de formularios con feedback** (`UX`, lote E): Peso corporal muestra error con `role="alert"` («Introduce un peso mayor que 0.») y borde rojo al guardar ≤ 0 (se limpia al escribir); en Ajustes, los porcentajes de calentamiento inválidos (fuera de 1–100, no numéricos, vacíos) avisan con `role="alert"` listando los valores rechazados en vez de descartarlos en silencio. Los switches de Ajustes (`Toggle`) ganan `aria-label` (antes sin nombre accesible). Verificado con Playwright.
- **Validación de números extremos/negativos en inputs** (`fix`): helper `clamp` en `src/domain/numberGuard.ts` aplicado en todos los inputs numéricos que persisten datos. En sesión (`SetRow`) el peso se acota a 0–1000 kg, las reps a 0–1000 y RPE/RIR a sus rangos (4–10 / 0–6); peso corporal se acota a 0–400 kg y sigue rechazando ≤ 0; ajustes: series a precargar 0–20, ajuste de peso 0–100, progresión 0.5–10 (ya existía) y deshacer 0–120 s; rutinas: series 1–20, reps 1–100 y descanso 1–600 s. Además: `calcBrzyckiOneRepMax` devuelve 0 con reps ≥ 37 (antes producía 1RM negativo/infinito) y `platesForWeight` limita el objetivo a 1000 kg (antes un input gigante colgaba la calculadora de discos en un bucle de ~4e14 iteraciones). Calculadoras ganan atributos `min`/`max` acordes. 13 tests nuevos (`numberGuard`, `oneRepMax`, `plates`).
- **Gráficos y marcas visibles aunque haya pocos datos (`fix`, UX)**: el panel «Volumen por semana» de Perfil y la sección «Evolución» de Peso corporal solo se montaban con ≥2 sesiones/registros y su aviso («Necesitas al menos 2 sesiones…», «Necesitas al menos 2 registros…») nunca llegaba a verse (código muerto). Ahora se muestran desde el primer dato, con su placeholder como pista de qué falta. «Mejores marcas» en Perfil pasa a mostrarse siempre, con un texto guía cuando no hay PRs («Completa series con peso para registrar tus mejores marcas.») en vez de desaparecer en silencio.
- **Series marcadas sin peso ya no se guardan en silencio (`fix`)**: al finalizar el entreno, si hay series completadas con peso 0, se avisa antes de guardar (confirm) porque no suman volumen ni marcas (con excepción explícita para ejercicios de peso corporal); `saveWorkoutSession` omite las series vacías (0 kg y 0 reps) y devuelve `skippedSets`, que el resumen de sesión muestra («N series vacías no guardadas»). Además el resumen informa de las series realmente guardadas (`completedSets` = series válidas).
- **Gráficos dibujan desde el primer registro (`fix`)**: `VolumeChart`, `E1rmChart` y `BodyWeightChart` ya no exigen 2 puntos para pintar — con una sola sesión/registro renderizan el punto (dot) del dato real en su gráfico (Área/Línea), en vez del aviso «Necesitas al menos 2…». El mensaje solo aparece cuando no hay datos (0 sesiones o, en el peso, 0 registros en el rango seleccionado).

### Changed
- **Accesibilidad WCAG 2.2 (lote A de la Fase 39)**: labels asociados (`htmlFor`/`id`) en IMC, Calorías, Macros y `CalculatorField`, en Peso corporal y en el builder de rutinas (nombre, día, series/reps/descanso, superserie); `aria-label` en reps de `SetRow`, búsqueda del `ExercisePicker` y textarea «Mi nota»; `aria-label` en botones solo-icono (cerrar picker, reiniciar descanso) y en `NumberField`/`Select` de Ajustes. `ExercisePicker` pasa a ser un diálogo real (`role="dialog" aria-modal="true"`, cierra con `Escape` y restaura el foco). Se eliminan los interactivos anidados (estrella de favorito fuera del `<Link>`/`<button>` en `EjerciciosPage` y `ExercisePicker`). Contraste modo día-gold: `accent`/`cta` pasan de `#b07f2e` a `#7a4f10` (3.55:1 → ~7:1 sobre blanco); placeholders más legibles (`muted/70`). `aria-pressed` en toggles de sexo/objetivo (Calorías, Macros). El hero de Home pasa de `<h1>` a `<h2>` (único `h1` = `AppHeader`). Contraste de iconos de papelera en noche (≥3:1). Verificado con typecheck, build, lint sin warnings nuevos y 27 tests.
- **Calendario semanal en Entrenar (`UX`)**: la semana vigente se muestra como una fila limpia de 7 días (letra del día + número en Oswald) con la fecha de hoy en la cabecera («6 agosto»); los días entrenados van en verde, los planificados por el programa activo en dorado (`accent-soft`) y hoy en círculo dorado. Todo el bloque es un enlace que lleva al calendario completo. Se elimina el título "Calendario" y el botón "Ver completo" de la home (la información extra queda en `/calendario`). Adaptado a las 5 paletas vía tokens (`WeekCalendar.tsx`). Verificado con Playwright.
- **Calendario semanal en Entrenar (`UX`)**: el bloque "Calendario" de la home pasa de mes completo a la semana vigente (lunes a domingo) con el nuevo `WeekCalendar` (`src/components/calendar/WeekCalendar.tsx`), que usa `buildWeekGrid` en `domain/calendar.ts` (reutiliza la misma lógica de estados que el mes) y resalta el día actual con anillo. Mantiene el enlace "Ver completo" a `/calendario`, que conserva la vista de mes con navegación. Verificado con Playwright (7 celdas en Home, 31 en Calendario).
- **Ficha de ejercicio con query dirigida (`optimize`)**: el gráfico e1RM de `/ejercicios/:slug` ya no carga todos los entrenos (`workoutRepo.getAll()`); nuevo método `workoutRepo.getMany(ids)` (`anyOf`) que solo recupera los workouts de los sets de ese ejercicio, deduplicados por `useMemo`. La UI no cambia.
- **Cálculos del Perfil memoizados (`optimize`)**: el mapa nombre→ejercicio (sobre el catálogo de 873 ejercicios), el volumen semanal, el volumen total, la señal de deload y el insight semanal se calculan con `useMemo` y solo se recomputan cuando cambian los entrenos (`PerfilPage`). Sin cambios de UI.
- **Historial de peso corporal virtualizado (`optimize`)**: la lista de registros de `/peso-corporal` usa `useWindowVirtualizer` (filas de `HistoryRow` memoizadas, `overscan`, `scrollMargin` medido por layout), igual que la biblioteca de ejercicios. Con ~300 registros el DOM pasa de ~4100 nodos a ~340 (8 filas montadas de 300); se mantienen el timeline, la línea vertical y el botón de eliminar por fila. Verificado con Playwright (scroll al fondo muestra el registro más antiguo y el borrado funciona).
- **Virtualización de la biblioteca de ejercicios (`optimize`)**: `/ejercicios` y el selector de añadir ejercicio de la sesión dejan de renderizar las 873 filas del catálogo de golpe. `EjerciciosPage` usa `useWindowVirtualizer` y `ExercisePicker` `useVirtualizer` (`@tanstack/react-virtual@^3.14.9`, con `overscan`, `gap` y `scrollMargin` medido por layout); las filas van memoizadas (`React.memo`) y el buscador tiene debounce de 150 ms (`useDebouncedValue`) con filtrado por `useMemo` + `Set` (`filterExercises` acepta ahora `ReadonlySet<number>`; favoritos en `useExerciseFavorites` con `useCallback`). Verificado con Playwright: el DOM pasa de 873 filas a 11 y la primera tecla de búsqueda de ~3000 ms a ~330 ms (debounce incluido).
- **Scrollbar oculta en filtros horizontales de Ejercicios** (`UX`): las 3 filas de chips de `/ejercicios` dejan de mostrar la barra de scroll nativa (`scrollbar-hidden` en `src/index.css` via `scrollbar-width: none` + `::-webkit-scrollbar { display: none }`) y mantienen el deslizamiento con dedo (scroll nativo con `touch-pan-x`) y con ratón (drag con cursor grab mediante `useDragToScroll`, sin `setPointerCapture` para no robar el `click` de los chips; el click resultante de un arrastre se suprime con `preventDefault`). Nuevo componente reutilizable `src/components/ui/HScroll.tsx`. Verificado con Playwright (scrollbar `none`, arrastre 0→140 px sin activar filtros, click normal activa/desactiva chips tras el arrastre).
- **Empty states** (`UX`, lote E): Perfil muestra tarjeta con CTA «Empezar a entrenar» cuando no hay entrenos; Guías muestra estado vacío si no hay guías; el detalle de rutina indica en días sin ejercicios «Este día aún no tiene ejercicios» con enlace al editor (solo rutinas propias); Rutinas distingue «No hay rutinas con estos filtros.» (con filtros activos) de «Aún no hay rutinas predefinidas» + hint a «Nueva rutina» (sin filtros). Verificado con Playwright (incluye borrado de guías vía IndexedDB y creación de rutina vacía).
- **Target sizes ≥44px** (`UX`, lote E, AGENTS.md): inputs de sesión (`SetRow`) de 40→44px y sus botones de completar/eliminar a 44px; chips de filtros de Ejercicios (30→44px); botones pequeños de `RutinaBuilderPage` (quitar día 40px, quitar ejercicio 32px) y de eliminar registro de Peso corporal (36px) a 44px; switches de Ajustes y del deload de Entrenar a 44px; selects/inputs numéricos de Ajustes y enlaces secundarios de Entrenar a 44px. Verificado con Playwright (bounding boxes 44px en Ajustes, Ejercicios, builder y Home).
- **Eliminación de dead code** (`refactor`, lote D): se borran `src/pages/CalculatorStubPage.tsx` y `src/components/ui/PagePlaceholder.tsx` (sin referencias en la app).
- **Consolidación de duplicados en `domain/` (`refactor`, Fase 39 lote D)**: labels de músculo, objetivo y nivel unificados en `src/domain/routines.ts` (`MUSCLE_GROUP_LABELS`, `OBJECTIVE_LABELS`, `LEVEL_LABELS`) y `slugify`; iconos/colores de objetivo en `src/components/routines/routineMeta.ts` (`OBJECTIVE_ICONS`, `OBJECTIVE_COLORS`); duración de sesión y volumen semanal en `src/domain/workouts.ts` (`workoutDurationMin`, `weeklyVolume`, `workoutDate`). Se eliminan las copias en `EntrenarPage`, `PerfilPage`, `EjercicioDetailPage`, `RutinasPage`, `RutinaDetailPage` y `RutinaBuilderPage`. Nota: el volumen semanal de Perfil pasa a usar `localDate` (como Home/timeline) en vez de `startedAt`, y la duración mínima se unifica a 1 min (`Math.max(1, …)`) donde una pantalla mostraba 0. Test runner Vitest configurado (`npm test`) con 14 tests en `src/domain/*.test.ts` que fijan el comportamiento.
- **Inputs numéricos sin flechas nativas**: se ocultan los spinners del navegador en todos los `input[type=number]` (`src/index.css`): `appearance: textfield` en el input + `-webkit-appearance: none` en `::-webkit-inner-spin-button`/`::-webkit-outer-spin-button` (Chrome/Edge/Safari) y `-moz-appearance: textfield` (Firefox). Quedan limpios y consistentes con el tema; el teclado numérico sigue disponible.
- **Historial unificado en timeline** (`F38`): el historial de entrenos se extrae a un componente compartido `WorkoutHistoryTimeline` (`src/components/workout/WorkoutHistoryTimeline.tsx`) con el diseño del timeline de Perfil (línea vertical, punto CTA, tarjeta con fecha + hora + volumen · duración, enlace al detalle). Se usa en Perfil ("Historial reciente") y en Home ("Historial reciente", misma tarjeta), y "Último entreno" del Home adopta la misma tarjeta con punto y borde dorado de destacado (fecha corta, hora, volumen y minutos). El "Historial" de Peso corporal aplica el mismo patrón de timeline (punto + tarjeta con fecha y peso + botón de eliminar). Fecha del día desde `localDate` (evita desfase UTC), hora desde `startedAt`.
- **Rediseño Industrial-premium (`F38`)** (skill `frontend-design`, dirección dorado+negro de alta gama):
  - *Atmósfera y profundidad*: `body` con washes radiales del CTA por paleta, overlay de grano sutil (`app-grain`) sobre la app, y sistema de superficie `panel` / `panel-hero` (borde, gradiente top-light, sombra profunda y glow CTA) en `index.css`. `AppShell` monta el grano y anima la entrada de cada ruta (`animate-page-in`).
  - *Tipografía de datos*: utilidades `kicker` (label uppercase espaciado), `stat-value` (Oswald condensado con `tabular-nums`) y `chip`; nuevo componente `CountUp` (animación con easing, respeta `prefers-reduced-motion`). Home y resumen de sesión muestran números hero en oro.
  - *Movimiento de alto impacto*: reveals escalonados (`reveal`, `reveal-1..5`), `RestTimer` rediseñado como arco circular SVG con countdown grande (danger + pulse en los últimos 3 s), y celebración de fin de sesión con ráfaga de partículas doradas (`burst-particle`) cuando hay PRs nuevos, en la pantalla de resumen.
  - *Home como hero asimétrico*: `EntrenarPage` gana un hero `panel-hero` con kicker ("Hoy toca"), día del programa sobredimensionado, chips de grupos musculares, ProgressRing y CTA principal dorado contextual (Continuar entreno / Empezar hoy / Iniciar / Ver rutinas); racha y volumen semanal pasan a paneles con count-up. Secciones del Home unificadas a `panel`.
  - *Sesión rediseñada*: header de progreso premium (`panel-hero`) con Volumen y Tiempo como `stat-value`, sets completados en dorado con glow (antes verde), badge PR con brillo, y "Finalizar entreno" con gradiente dorado.

### Changed
- **Reseed fuera de `Providers` y limpieza (`refactor`)**: el reseed versionado sale de `src/app/providers.tsx` (que queda solo como boot gate) y vive en `src/data/seed/reseeder.ts` (`ensureSeeded`). Se elimina el bypass de `db` en `useExerciseNotesMap` (nuevo `exerciseNoteRepo.getAll`) y en `useBodyWeight`, y se borra `useBodyWeightRaw`, `useRecentWorkouts`, el `toLocalDateStr` duplicado en `bodyWeightRepo` y el módulo muerto `src/theme/tokens.ts`.
- **Inicio de sesión unificado (`refactor`)**: nuevo `useStartSession` (`src/hooks/useStartSession.ts`) que funela el flujo `loadLastSets → buildSets → store` de empezar un entreno. `EntrenamientoPage.handleAddExercise` y `RutinaDetailPage.handlePlay` lo usan (`startFreeExercise` / `startRoutineDay`), eliminando la duplicación entre ambos caminos.
- **Guardado de sesión fuera de la página (`refactor`)**: el guardado al finalizar un entreno deja de vivir en `EntrenamientoPage.handleFinish` y pasa a `src/data/workoutSession.ts` (`saveWorkoutSession`, escribe workout + sets + PRs de un snapshot) orquestado por el hook `useFinishWorkout`. `activeWorkoutStore` pierde `finishWorkout`: ya no hay reset-before-return, la sesión se conserva si el guardado falla (se puede reintentar sin perder datos) y la página elimina ~75 líneas de lógica en el event handler. La detección de PRs usa `detectPRsFromSets` (mejor serie por ejercicio), así que el resumen cuenta un PR por ejercicio con marca nueva en vez de una por serie.
- **Lógica de sesión por funciones de dominio (`refactor`)**: la sesión activa y el header de `/entrenamiento/active` ahora calculan volumen, series y progreso con `computeSessionStats` (`src/domain/sessionProgress.ts`, reutiliza `countSessionSets` y `calcTotalVolume` de `src/domain/volume.ts`) en vez de reimplementaciones inline. `activeWorkoutStore.finishWorkout` usa esa misma función y se elimina su copia local de volumen. El flag "PR" de cada serie en `ExerciseBlock` pasa a `isPR` (`src/domain/prs.ts`) con la fórmula 1RM (Brzycki redondeada) de una sola fuente, evitando que la UI y el PR guardado discrepen.
- **Rendimiento de carga (`optimize`)**: code-splitting por ruta con `React.lazy` + `Suspense` en `src/app/router.tsx` (las 27 páginas se cargan bajo demanda; fallback `Loader` con estado `role="status"` en `AppShell` mantiene la TabBar visible). Vendor chunks vía `codeSplitting.groups` en `vite.config.ts`: `vendor-react`, `vendor-router`, `vendor-dexie`, `vendor-charts` (Recharts, solo en Perfil/Detalle ejercicio/Peso corporal) y `vendor-icons`. El chunk inicial pasa de 1201 kB → 350 kB (302 kB → 53 kB gzip) y desaparece el aviso de >500 kB; cada vendor se precachea y cachea por separado en la PWA.

### Fixed
- **Gráfico de volumen semanal en Perfil**: la condición para mostrar el gráfico exigía al menos 2 *semanas* distintas con datos (`data.length >= 2`), así que con varias sesiones dentro de la misma semana mostraba "Necesitas al menos 2 sesiones" y no dibujaba nada. Ahora se evalúa el número de sesiones (`workouts.length >= 2`) y el `Area` renderiza el punto único con `dot`/`activeDot` cuando solo hay una semana registrada.
- **Onboarding y ajustes entre reseeds**: el reseed (`providers.tsx`) limpiaba toda la tabla `meta`, con lo que `onboardingDone` y `settings` se perdían en cada cambio de `SEED_VERSION` y el wizard de primera visita reaparecía al usuario. Ahora se preservan las filas de `meta` existentes y solo se sobreescribe `seedVersion`.

### Added
- **Insights de progreso (`F37`)**: tarjeta `InsightCard` que compara el volumen de esta semana vs la anterior usando `computeWeeklyVolumeInsight` en `src/domain/insights.ts` (puro, agrupa por semana calendario como `VolumeChart`, umbrales +5% alza / −10% descenso, sin baseline → neutral). Tres variantes de tono: positiva (volumen al alza), alerta (volumen en descenso) y neutra (estable / punto de partida). Montada en Home bajo la tarjeta "Volumen sem." y en Perfil junto al gráfico de volumen. Se muestra solo con ≥2 sesiones y datos en alguna de las dos semanas.
- **Asistente de carga inteligente (`F36`)**: botón "Sugerido: X kg" en cada `ExerciseBlock` de la sesión que propone el peso de la siguiente serie incompleta y, al tocarlo, lo aplica al campo de peso. `src/domain/loadSuggestion.ts` (puro): `suggestNextLoad` ancla en `max(peso mejor serie completada, PR kg)`, aplica progresión configurable con factor por RIR (RIR ≥ 2 acelera ×1.5, RIR ≤ 1 frena ×0.5) y redondea a plato de 2.5 kg; `bestCompletedSetWeight` y `roundToPlate`. Hook `src/hooks/useLoadSuggestion.ts` (combinando sesión activa + PR + ajustes). Nuevos ajustes en Ajustes → Sesión: toggle "Sugerir carga" (`showLoadSuggestion`, default on) y "Progresión (%)" (`loadProgressionPct`, default 2.5, rango 0.5–10). Sin tocar las series de calentamiento.
- **5 paletas × modo día/noche (`F35`)**: selector de color principal (Dorado, Energía, Carmesí, Eléctrico, Violeta) con swatches en Ajustes → Apariencia, independiente del modo Noche/Día. Cada combo redefine `accent`, `accent-soft`, `gold`, `gold-bright`, `cta`, `cta-deep`, `border`, `muted`, tinte de `bg-elevated` y `--color-on-gold` (10 variantes en `index.css`). `useTheme` gana `palette`/`setPalette` (persistencia `localStorage` `gymlab.palette` + `meta.palette`), el anti-flash de `index.html` aplica `data-palette` antes del paint, `useThemeColors` observa `data-palette` (gráficos Recharts reaccionan) y el `<meta name="theme-color">` se actualiza al CTA activo. `.gold-gradient`/`.gold-text`/`.gold-border-glow` ahora usan `var(--color-*)`. `text-black` sobre CTA → `text-on-gold` en `ConversorPage` y skip-link de `AppShell`. Default = Dorado + Noche (comportamiento actual).
- **Plan F36/F37** (`docs`): nuevas fases planificadas en `PLAN.md` — F36 Asistente de carga inteligente (sugiere el peso objetivo de la siguiente serie con e1RM/PR/RIR) y F37 Insights de progreso (comparativa de volumen semanal). Sin cambios de código aún.
- **Seed 5/3/1 Wendler + definición (`F33b`)**: rutina nueva `531-wendler` (4 días: press militar, peso muerto, banca y sentadilla, cada uno con serie 5/3/1 de 3×5, complemento BBB 5×10 y accesorios) y mejora de `PPL Definición` con un 5º ejercicio por día (fondos en paralelas, remo a una mano y hip thrust) manteniendo el foco de densidad y poco descanso. `SEED_VERSION` → `10`.
- **Guías cortas (`F33c`)**: 6 guías nuevas en `seedGuides` — HIIT vs LISS, estancamiento, semana de deload, espalda segura, menú de definición y sobreentrenamiento (ids 8-13) — con bullets accionables y el disclaimer "Informativo, no consejo médico" que ya muestra la ficha de guía. `SEED_VERSION` → `11`.
- **Plantillas de 1 día (`F33d`)**: 8 sesiones sueltas nuevas en el seed (`pecho-15`, `espalda-casa`, `abs-principiante`, `pierna-express`, `gluteo-express`, `fullbody-20`, `brazos-hombros`, `cardio-core`) con `daysCount: 1`. En `/rutinas` llevan badge "Sesión suelta" (verde), muestran "Sesión suelta" en vez de "X días/semana" y hay un filtro Tipo (Todas / Sesión suelta / Programa). `SEED_VERSION` → `12`.
- **Calculadora de macros (`F34a`)**: `src/domain/calculators/macros.ts` (puro) con `calcMacros`, que parte del TDEE (Mifflin-St Jeor) y aplica factor por objetivo (volumen ×1.1, mantenimiento ×1, definición ×0.8), proteína 1,8–2,2 g/kg, grasas 0,8 g/kg y el resto de calorías como carbohidratos. Nueva página `/calculadoras/macros` (MacrosPage, reutiliza `CalculatorField`) con selector de objetivo y tarjetas de kcal + proteína/carbs/grasas, entrada en el hub y disclaimer.
- **Calculadora de discos en hub (`F34b`)**: entrada "Calculadora de discos" en `/calculadoras` que abre el `PlateCalculatorModal` existente (discos por lado para una carga), sin necesidad de ruta propia.
- **Superserie en sesión UX (`F34c`)**: en `/entrenamiento/active` los bloques del mismo `supersetGroup` se agrupan visualmente (contendor con borde, cabecera "Superserie A/B" y badge "Completada" cuando todos los sets del grupo están hechos), y al completar el grupo activo la vista hace scroll automático al siguiente grupo incompleto. `EntrenamientoPage` agrupa con `useMemo` (`groupExercises`) y mantiene refs por grupo para el auto-foco.
- **Rutinas con jerarquía y badge de categoría (`F34d`)**: `/rutinas` agrupa las predefinidas por objetivo (Volumen, Definición, Fuerza, Resistencia, General) en secciones con icono y color, y cada card muestra un badge del objetivo junto al de "Sesión suelta"/"Propia". En `/rutinas/:slug` la ficha muestra pills de categoría (objetivo), frecuencia (días/semana o Sesión suelta) y nivel.
- **Microfeedback de serie + peak del RestTimer (`F34d`)**: al completar una serie, el botón de check hace un "pop" (`animate-pop`) y la fila hace un destello verde (`animate-row-flash`) en `SetRow`. El `RestTimer` detecta el final del descanso: en los últimos 3 s el contador y la barra pasan a rojo y pul san (`animate-timer-peak`), reforzando el aviso sonoro/vibración.
- **Historial en timeline (`F34d`)**: en Perfil, "Historial reciente" pasa de lista plana a timeline vertical con línea y punto de evento; cada entrada muestra fecha, hora, volumen y duración en minutos, y sigue enlazando al detalle de la sesión.
- **Polish de hubs y contraste (`F34d`)**: `/calculadoras` pasa de lista a grid de 2 columnas con tarjetas de altura uniforme (128 px, descripciones con `line-clamp-2`) y la "Calculadora de discos" ocupa ancho completo; `/mas` reordena los accesos (Perfil e historial primero, Ajustes al final); los tooltips de `VolumeChart`, `E1rmChart` y `BodyWeightChart` fijan `labelStyle`/`itemStyle` con los tokens del tema para garantizar contraste en modo día.

### Added
- **Historial de sesión con detalle (`F32b`)**: nueva vista de solo lectura en `/entrenamiento/:id` (`WorkoutDetail` + wrapper `SesionPage`) con fecha, duración, volumen, series completadas, notas y desglose por ejercicio (serie, peso × reps, RPE, calentamiento). Las filas de historial en Home (`EntrenarPage`) y Perfil ahora enlazan al detalle. Empty state si la sesión no existe.
- **Gráfico de evolución 1RM (`F32c`)**: serie temporal de e1RM por ejercicio en la ficha `/ejercicios/:slug` (`E1rmChart` con Recharts y tokens del tema). Nuevo `buildE1rmSeries` en `src/domain/e1rm.ts` (reutiliza `estimate1RM`, mejor valor por sesión) y método `workoutSetRepo.getByExercise`.
- **Semana de deload en programa activo (`F32d`)**: toggle en Home y CTA "Activar semana de deload" en el banner del Perfil (cuando hay programa activo). `ActiveProgram` gana `deloadActive`/`deloadUntil` (+7 días), helper `isDeloadActive` en `src/domain/deload.ts` y `activeProgramRepo.setDeload`. Badge "Semana deload" en la tarjeta de programa.
- **RIR opcional (`F32e`)**: columna "RIR" en `SetRow` (0–6) activable desde Ajustes (`showRir`), persistida en `WorkoutSet.rir` y mostrada en el detalle de sesión.
- **Onboarding de primera visita (`F32f`)**: wizard de 3 pasos (objetivo → días/lugar/nivel → rutina sugerida) con chips, flag `onboardingDone` en meta, sugerencia de rutina por objetivo+días (`suggestRoutine` en `src/domain/onboarding.ts`), CTA "Empezar D1" que configura el `activeProgram`, y skip "Ya entreno aquí". No se re-muestra si ya hay sesiones.
- **Home como dashboard (`F32g`)**: CTA fuerte a `/rutinas` cuando no hay programa activo (copy distinto si aún no hay sesiones), y el bloque "Hoy toca" muestra el día + los grupos musculares del día programado.
- **Pack mujer/glúteo (`F33a`)**: 4 rutinas nuevas en el seed extraídas de la biblioteca de mujer — `mujer-2d` (tren sup/inf), `mujer-3d` (sup/inf/full body), `mujer-4d` (empuje/tirón) y `gluteos-3d` (glúteos con torso intercalado) — todas con `exerciseId` del seed (< 1000), y la guía **Glúteos base** en la categoría mujer. `mujer-full-3d` renombrada a "Full Body Mujer 3 días". `SEED_VERSION` → `9`.

### Changed
- **Unidades kg/lb en toda la UI (`F32a`)**: la sesión de entrenamiento (`SetRow` input con placeholder y `aria-label` en la unidad elegida, guardando siempre kg), PRs y e1RM (`ExerciseBlock`, `PerfilPage`), volúmenes de resumen/sesión/home/perfil, tooltip del `VolumeChart`, y chips de discos en `PlateCalculatorModal` ahora muestran la unidad activa (`formatUnits`/`applyUnits`/`formatWeight`/`parseWeightToKg`). Se elimina la importación duplicada en `EntrenarPage`.

### Added
- **Licencia propietaria**: `LICENSE` en la raíz del repo con **All Rights Reserved** (source-available, no open source); `"license": "UNLICENSED"` en `package.json`, sección de licencia en README raíz y de la app. El material de terceros (free-exercise-db Unlicense, paquetes npm) conserva sus propios términos.
- **Auditoría de skills** (`F29-cierre`): skip link “Saltar al contenido” en `AppShell`, `document.title` dinámico por página en `AppHeader` (“{Página} · GymLab”), y revisión de arquitectura/SEO/site según las skills del repo (sin páginas huérfanas; `lang="es"`, meta y manifest ya presentes).
- **Dummy muscular en ficha** (`F29`): `MuscleDummy` acepta `highlight` y resalta el músculo trabajado en rojo (`danger`) en `/ejercicios/:slug`.
- **a11y (`F29`)**: `:focus-visible` global con anillo CTA, `aria-label` en búsqueda/campos de calculadora, `aria-pressed` en chips.
- **Calculadoras nuevas (`F29`)**: **1RM** (Brzycki + Epley), **Agua diaria** (35 ml/kg + recarga) y **Conversor lb ↔ kg**, con domain puro en `src/domain/calculators/{oneRepMax,water,converter}.ts`, páginas y rutas `/calculadoras/{1rm,agua,conversor}`; hub actualizado.
- **Catálogo JSON versionado** (`F28`): los 821 ejercicios extra ahora se publican en `public/catalog/exercises-v1.json` y se cargan con fallback al seed embebido (`src/data/catalogLoader.ts`). Corregidos ~100 nombres auto-ES absurdos vía `src/data/seed/translations.ts` (override por slug, conserva el inglés técnico). `SEED_VERSION` → `7`.
- **Mini-calendario en Entrenar**: componente compartido `MonthCalendar` (mes actual compacto bajo la racha con días hechos/programados) y link “Ver completo” a `/calendario`; `CalendarioPage` reutiliza el mismo componente.
- **Modo noche/día**: tema dual `data-theme="night|day"` (negro o blanco + dorado), token `--color-on-gold`, hook `useTheme` con persistencia `localStorage` + `meta.theme`, anti-flash en `index.html` y página **Ajustes** (`/ajustes`) con toggle desde Más.
- **Rutinas custom**: builder en `/rutinas/nueva` y edición en `/rutinas/:slug/editar` (solo propias), CRUD (`createRoutine`/`updateRoutine`/`deleteRoutine`) con cascada days+items e ids ≥ 10000, secciones **Mis rutinas / Predefinidas** con badge “Propia” en Rutinas, y acciones Editar/Eliminar en el detalle de rutinas propias. El reseed preserva las rutinas custom.

### Added (legacy)
- Biblioteca de contenido offline en `content/training-library/` (referencia para seeds/guías).
- **Dexie v2**: `guides`, `activeProgram`, `meta` (seed versionado), tablas social stub (`socialProfiles`, `posts`, `postMedia`).
- Domain: fechas locales, calendario, progreso de sesión/programa, fatiga muscular, payload social.
- Seeds ampliados: 52 ejercicios, 12 rutinas, 6 guías.
- UI **ProgressRing** (sesión + programa en home).
- **Calendario** (`/calendario`): días hechos vs programados.
- **Cuerpo** (`/cuerpo`): dummy muscular + fatiga por grupo.
- **Guías** (`/guias`): nutrición, macros, recuperación.
- Detalle rutina: Play con ETA, precarga series, “Seguir rutina” (ActiveProgram).
- Sesión: finalizar ejercicio, anillo de progreso, `localDate` al guardar.
- **ExerciseMedia** + SVG placeholders por grupo muscular.
- Imágenes reales de ejercicios (2 frames por ejercicio) desde **free-exercise-db** (Unlicense/dominio público) en `public/exercises/{slug}/`, con atribución en README y en Más. `SEED_VERSION` → `4` para re-sembrar con las nuevas `imageUrls`.
- **Catálogo completo de free-exercise-db**: 821 ejercicios extra (873 total) con 2 fotos reales cada uno (1.746 JPG en total) en `src/data/seed/exercisesCatalog.ts` (`seedExercisesExtra`, ids 1000+). Nombres en español híbrido generados con `build-catalog.cjs` (traducción automática fiable; los complejos/técnicos se mantienen en inglés). `SEED_VERSION` → `5`.
- Logo cabecera más grande (`size-12`).
- Scaffold **Vite + React + TypeScript** en `gymlab-app/`.
- **PLAN.md** — plan de fases (stack, arquitectura, site map, calculadoras, Capacitor).
- **AGENTS.md** — reglas para agentes (capas, stack, convenciones, rutas, obligación de actualizar changelog).
- **CHANGELOG.md** — este archivo.
- Dependencias: `react-router-dom`, `dexie`, `dexie-react-hooks`, `zustand`, `recharts`, `lucide-react`, Tailwind v4 (`@tailwindcss/vite`), `vite-plugin-pwa`.
- Tema oscuro GymLab (tokens CSS + Barlow / Barlow Condensed).
- **AppShell** + **TabBar** mobile-first: Entrenar · Rutinas · Papers · Más.
- Rutas base: `/`, `/entrenamiento/:id`, `/rutinas`, `/papers`, `/mas`, `/perfil`, `/calculadoras`, `/calculadoras/:calcId`, `/ejercicios`.
- Páginas placeholder con UI de marca para cada sección.
- Hub **Calculadoras** (IMC y TDEE listados; roadmap 1RM, macros, agua, % grasa, conversor).
- PWA mínima (manifest + service worker autoUpdate).
- Alias `@` → `src/` en Vite.
- Repositorio Git en la raíz del monorepo, remoto `origin` → https://github.com/Yvesdefaria/GymLab.git (`main`).
- **Fase 2 completada:** Domain + Data layer.
  - Tipos TypeScript: Exercise, Routine, Workout, WorkoutSet, Paper, Profile, PRRecord.
  - Domain: `calcSetVolume`, `estimate1RM`, `detectPRsFromSets`, `calcStreak`.
  - Calculadoras domain: IMC (OMS) y TDEE (Mifflin-St Jeor).
  - Dexie schema v1: exercises, routines, routineDays, routineItems, workouts, workoutSets, papers, profile, prs.
  - Repositories interfaces + Dexie implementations (exercise, routine, workout, workoutSet, paper, profile, pr).
  - Seed data: 40+ ejercicios, 8 rutinas (PPL, Upper/Lower, Full-Body, 5×5, Bro Split, Starting Strength, Torso/Pierna), 6 papers con DOI reales.
  - Providers con auto-seed al primer arranque.

### Changed
- **Rutinas predefinidas sin duplicados** (`F31-seed`): eliminadas `Upper/Lower 4 días` (id 2) y `Full Body 3x` (id 3) — réplicas en inglés de `Torso/Pierna 4 días` y `Full Body 3 días` — junto con sus días e ítems en el seed (`src/data/seed/routines.ts`). Quedan 10 rutinas únicas; sin nombres repetidos ni traducciones del mismo programa. `SEED_VERSION` → `8`.
- **Resumen de entreno rediseñado (Peak-End)**: aplicada la skill `mobile-app-ui-design` a la pantalla de finalizar sesión. Hero celebratorio con glow + icono Trophy/Flame según PRs/racha, headline y copy de refuerzo dinámicos, grid de 4 stat cards (Volumen, Series, Duración, PRs/Racha con resaltado CTA), CTA primario “Volver al inicio” + secundario “Ver mi progreso”, y microcopy de cierre. Regla Peak-End de Kahneman: el momento cumbre y el cierre de la sesión ahora se sienten premiados.
- **Empty states según la skill `mobile-app-ui-design`** (`F31b`): `MonthCalendar` muestra un estado vacío con icono, copy motivacional y CTA “Empezar ahora” cuando el mes no tiene entrenos (variante “Este mes no hay entrenos” si ya hay historial); la ficha de ejercicio (`/ejercicios/:slug`) añade la tarjeta “Mi mejor marca” con peso × reps, fecha y 1RM estimado (regla Vanity Mirror), o un empty state “Sin historial todavía” con CTA a iniciar entreno; el placeholder de `ExerciseMedia` usa ahora un monograma con la inicial del ejercicio en un frame estable `4/3`.

### Fixed
- **Overflow horizontal en Ajustes a 320px** (`F31b`): las filas condicionales “Series a precargar / Ajuste de peso / Porcentajes (%)” con `select`/`number` ahora usan `flex-wrap`, evitando que select + input se salgan de la tarjeta en pantallas estrechas.

### Fixed
- **Guardado de entrenos roto (`DataError`)** (`F28-bug`): `workouts` y `workoutSets` declaran `id` sin `++` en el esquema Dexie, así que `db.workouts.add(...)` sin `id` lanzaba `DataError` y “Finalizar entreno” acababa en la pantalla vacía sin guardar nada (por eso Perfil no actualizaba racha/volumen/PRs). `create()` ahora genera el `id` manualmente (`orderBy('id').last() + 1`, mismo patrón que `bodyWeightRepo`) sin tocar la primary key, evitando `UpgradeError` en bases existentes. Flujo sesión → resumen → home/Perfil verificado reactivo vía `useLiveQuery`.
- **Knob de los toggles de Ajustes centrado**: el círculo del `role="switch"` se re-posiciona con `left-1` + `translate-x-6` y píldora `h-8 w-14`, quedando con margen simétrico (4px) en ambos estados y centrado verticalmente.
- **Reseed con home en blanco** (`F28-bug`): el fetch del catálogo (`loadCatalog()`) se ejecutaba dentro de `db.transaction(...)` en `providers.tsx`, provocando `Transaction committed too early` y “Error de carga” hasta borrar datos. Ahora el catálogo se carga antes de abrir la transacción.
- **Nombre de ejercicio no enlazado en detalle de rutina**: cada ítem de día en `RutinaDetailPage` ahora resuelve el `slug` y enlaza a `/ejercicios/:slug` (hover subrayado, `truncate`).
- Racha y stats del home con fechas **locales** (evita desfase UTC).
- Tipado de `end` en `TabBar` (union `as const` con React Router).
- Warning Vite: `__dirname` sustituido por `import.meta.url`.
- Tipado de repositorios Dexie (`PromiseExtended` → `Promise<unknown>`).
- Alias `@/` en tsconfig (paths sin baseUrl en TS 6).

### Added (Fase 3 — Entrenar)
- **activeWorkoutStore** (Zustand + persist): sesión activa con exercises, sets, rest timer.
- **SetRow**: input peso/reps, checkbox completada, detección PR, borrar serie.
- **RestTimer**: barras de progreso, presets 30s–3m, play/pause/reset.
- **ExerciseBlock**: agrupa sets por ejercicio, PR badge, añadir/eliminar serie.
- **ExercisePicker**: modal con búsqueda + filtro por grupo muscular.
- **EntrenarPage**: botón iniciar/continuar, racha, volumen semanal, historial reciente.
- **EntrenamientoPage**: stats en vivo, finish → guarda en Dexie + actualiza PRs.
- Hooks: `useWorkouts`, `usePRs`, `useStreak` (dexie-react-hooks).

### Added (Fase 4 — Rutinas)
- **RutinasPage**: catálogo con filtros por objetivo (volumen/definición/fuerza/resistencia/general) y nivel (principiante/intermedio/avanzado).
- **RutinaDetailPage**: descripción, selector de días, lista de ejercicios con series/reps/descanso, botón "Iniciar entrenamiento" que precarga la rutina en el store.

### Added (Fase 5 — Papers)
- **PapersPage**: lista de papers con filtro por tema (hipertrofia, nutrición, entrenamiento, recuperación), links PubMed externos.
- **PaperDetailPage**: resumen, puntos clave, DOI, enlace a fuente oficial, disclaimer informativo.

### Added (Fase 6 — Perfil + Calculadoras)
- **PerfilPage**: tarjeta de usuario, estadísticas (racha, volumen semanal, total, PRs), gráfico de volumen semanal (Recharts AreaChart), historial reciente, lista de PRs.
- **VolumeChart**: gráfico de área con volumen por semana (Recharts).
- **ImcPage**: calculadora IMC con escala visual OMS, categorías coloreadas.
- **CaloriasPage**: calculadora TDEE (Mifflin-St Jeor) con selects de actividad, resultados déficit/superávit.

### Added (Fase 7 — Polish + Biblioteca)
- **EjerciciosPage**: biblioteca de 40+ ejercicios con búsqueda y filtro por grupo muscular.
- **EjercicioDetailPage**: ficha con técnica e información del ejercicio.
- HTML lang="es", meta theme-color, viewport-fit=cover para PWA nativa.

### Changed (Rediseño dorado)
- Paleta alineada con la guía real del prototipo (extraída de `GymLab/css/*`): CTA naranja `#F97316` sustituido por dorado `#D9B384`; acentos `#D9B384` / `#FDDDB4`; bordes y grises con tono cálido.
- Fuente display cambiada a **Oswald** (la del prototipo); body Barlow.
- Logo del prototipo añadido en `public/logo.jpg` y mostrado en los headers principales (Entrenar, Rutinas, Papers, Perfil) vía `AppHeader showLogo`.
- Botones CTA principales ahora usan gradiente dorado (`gold-gradient`) con sombra (Entrenar, RutinaDetail, Entrenamiento, RestTimer).
- TabBar activo con texto degradado dorado (`gold-text`).
- `theme_color` (meta + manifest) → `#D9B384`.
- `VolumeChart`, IMC "sobrepeso" y tokens → `#D9B384` (sin naranja en todo el app).

### Changed (Rediseño tarjetas, filtros y UX)
- **Tarjetas** de todas las páginas (Entrenar, Rutinas, Papers, Ejercicios, Perfil, Calculadoras, Más, detalle de sesión) con borde dorado `border-gold/40` y hover `hover:border-gold/80`, estilo del prototipo (botones oscuros `#242422` + borde `#D9B384`).
- **Chips/filtros seleccionados**: sustituido relleno `bg-cta` por borde dorado `border-cta` + tinte `bg-cta/20` + texto `text-accent-soft` (visible sobre fondo oscuro); hover dorado en no seleccionados.
- **TabBar**: icono activo con `text-cta` sólido (antes `gold-text` ocultaba el icono SVG por `color: transparent` → se veía negro); el degradado dorado queda solo en la etiqueta.
- **Entrenar**: botón "Iniciar entrenamiento" ahora navega a `/entrenamiento/active` tras `startWorkout()` (antes solo reseteaba estado, no funcionaba); CTA movido a barra fija justo encima del TabBar para acceso con el pulgar.
- **RutinaDetailPage**: "Iniciar entrenamiento" navega a la sesión; detección de entreno en curso por `startedAt` (antes por ejercicios cargados, fallaba al iniciar con lista vacía).
- Hovers de botones secundarios (añadir serie, añadir ejercicio, descanso) a dorado.

### Changed (Cabecera unificada)
- **AppHeader**: altura y posición idénticas en todas las páginas. Ahora siempre muestra la fila del logo (antes solo en Entrenar/Rutinas/Papers/Perfil) y reserva altura fija para título + subtítulo (`min-h-[3.5rem]`), con `truncate` para títulos largos. Eliminado el prop `showLogo`.
- **Desplazamiento lateral fijo**: `scrollbar-gutter: stable` en `index.css` para que la barra de scroll vertical no empuje el contenido centrado hacia la izquierda al navegar entre páginas con distinta altura (p.ej. Entrenar/Más sin scroll frente a Rutinas/Papers con scroll).

---

## Cómo actualizar

Al cerrar una fase o feature:

1. Añadir entradas bajo `[Unreleased]` (`Added` / `Changed` / `Fixed` / `Removed`).
2. Al hacer release: renombrar a `## [x.y.z] - YYYY-MM-DD` y dejar `[Unreleased]` vacío.
3. Marcar checkboxes correspondientes en `PLAN.md`.
